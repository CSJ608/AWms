# 第 3 批联调汇报（真实后端冒烟结果）

> 出具人：前端（Hermes）｜日期：2026-08-11｜分支：`feat/frontend-3-integration`
> 依据：协调者《前端联调指令 — 第3批（批末联调）》
> 角色：前端只联调、只报告；**未合并、未 push main**；待协调者抽查。

## 一、环境与版本

| 项 | 值 |
|---|---|
| 前端工作区 | `C:\build\f`（分支 `feat/frontend-3-integration`，基于 `origin/main@cbabdfc`） |
| 前端源码来源 | `feat/frontend-3-rework` 的 `frontend/`（F-R1/F-R2 修复版；main 上 frontend/ 仅 .gitkeep，协调者合并 main 时需并入前端源码） |
| Node / pnpm | v22.23.2 / 11.10.0（pnpm-lock 冻结安装成功） |
| Vite | 8.2.1（dev `http://localhost:5173`，`VITE_USE_MOCK=false`） |
| 后端 | `C:\build\b` @ **a696455**（`feat/backend-3-rework-2`，122 用例全绿版） |
| 后端启动 | `dotnet run --project backend/AWms.Api --no-launch-profile`，监听 `http://localhost:5000` |
| PostgreSQL | Docker `postgres:16-alpine`（16.14），容器 `awms-pg`，**端口 5433**（本机 5432 被用户私有 postgresql-server 容器占用，故映射 5433；连接串 `Host=localhost;Port=5433;...`） |
| 迁移/初始化 | 启动自动应用 `InitialCreate` + 初始管理员 `admin`（密码由环境变量注入 `Admin@2026Dev`） |
| 前端代理 | `vite.config.ts` 新增 `server.proxy /api → http://localhost:5000`（可用 `VITE_API_PROXY_TARGET` 覆盖） |

环境变量注入（指令要求"一律环境变量注入"）：`ConnectionStrings__Default`、`AWMS_JWT_SECRET`、**`Jwt__SecretKey`（见问题 Q6）**、`Admin__Username`、`Admin__Password`。

## 二、冒烟清单逐条结果

### 1. 登录/会话 ✅
- admin 登录 200：`token/expiresAt/user/permissions/menus.web+pda` 齐备（契约认证权限 v0.2 形态）。
- 错误密码 401 `LOGIN_FAILED`；登出 204 → 前端回登录页（浏览器实测）。
- `/auth/refresh`（过期 token 换新）：手工构造过期 JWT（正确签名）→ 200 返回新 `token/expiresAt`，新 token 可用（me=200）；错误签名/垃圾 token → 401。**5/5 PASS**。
- 登出后旧 token 仍可访问（`me`=200）：后端为**无状态实现**，契约 2.4 允许（"实现可无状态"），非缺陷。
- 前端 401 单飞刷新逻辑由 `refresh-queue` 单测 3 用例覆盖 + 后端 refresh 端点实测通过；浏览器端"过期 token 自动换新重放"未做全链路（见遗留项）。

### 2. 用户/角色/权限 ✅（1 项待裁决见 Q2/Q3）
- `GET /api/users?keyword=&page=&pageSize=` 分页正常；新建用户 201；重复用户名 409 `USERNAME_DUPLICATED`。
- `PUT /api/roles/{id}/permissions` 分配权限生效（分配后 GET /roles 权限点已更新）；`GET /api/permissions` 只读、13 个权限点、按 moduleCode 排序。
- `GET /api/roles` 返回**数组**（非 `{items,total}`，契约未明确形态，见 Q5）。
- OPERATOR（wang02）写端点 → **403 FORBIDDEN** ✅（`{"code":"FORBIDDEN","message":"缺少操作权限：action.material.create"}`）。
- 注意：联调期间曾给 OPERATOR 角色临时加 master-data 权限用于测试，**已恢复默认**（仅 inbound 3 码）。

### 3. 主数据 CRUD ✅
- 物料：新建 201（中文名/英文名均正常）；重复码 409 `MATERIAL_CODE_DUPLICATED`；编辑 200（**code 只读**：PUT 带 code 字段被忽略，code 不变）；删除 204，重复删除 404。
- 仓库：新建 201（status 枚举 `ENABLED/DISABLED`，脚本曾误用 ACTIVE 被后端正确拒绝 400）；重复码 409 `WAREHOUSE_CODE_DUPLICATED`；库位 201、**仓内重复码 409** `LOCATION_CODE_DUPLICATED`；嵌套列表 `POST /warehouses/{id}/locations/search` 200、仓库不存在 404。
- 来源：新建 201/删除 204/重复删除 404；`(type,code)` 唯一语义正常。
- **searchCode 唯一**（后端约束）：SM01 被占用后新建报错——契约物料.md 未写明 searchCode 唯一性，建议契约补充（记入遗留）。

### 4. 列表查询（契约 v1.9）✅
- `POST /api/materials/search`：keyword 命中 code/name/searchCode；filter DSL（name contains 中文）生效；**白名单外字段 400** `VALIDATION_ERROR`；sort 白名单内 200、白名单外 400。
- `GET /api/materials?keyword=&pageSize=` 快捷搜索 200（引用选择器用）。
- `POST /warehouses/search`、`/sources/search`、嵌套 `/warehouses/{id}/locations/search` 均 200。
- 运行时元数据 `GET /api/meta/fields/{resource}`：materials/warehouses/sources/batches/locations 全 200（浏览器筛选区据此动态渲染）；**users 404**（见 Q4）。

### 5. 批次 ✅
- `POST /api/batches/search` 默认 `createdAt DESC`（契约 v0.7 + 2.10 v2.1）——本期库内无批次（收货第 4 批才生成），排序断言以空列表+契约条款记录；时间降序逻辑由后端集成测试覆盖（122 用例绿）。
- `GET /api/batches?keyword=` 快捷搜索 200。
- 嵌套 `POST /api/materials/{id}/batches/search` 200；物料不存在 → **404** ✅。
- 批次页筛选元数据含 `materialCode`（Q-01 裁决删的是 materialName，materialCode 保留，符合）。

### 6. 导入（全链路 16/16 PASS）✅
- 模板下载 `GET /import-export/templates/materials` 200（xlsx，表头：物料编码/物料名称/助记码/批次管控/标签类型/默认单位/默认每签数量）。
- precheck 干净文件 → `canExecute=true`；重复码文件 → `failCount=1`、inline 失败明细含 `MATERIAL_CODE_DUPLICATED`、`canExecute=false`；**失败明细 inline ≤200** 满足。
- 失败任务 execute → 422 `IMPORT_VALIDATION_FAILED` 拒绝 ✅（"全部通过才可执行"）。
- 干净任务 execute → 200 `status=DONE` → **库中核对：POST /materials/search 查到 2 条新物料，字段正确**（与界面/库一致）。
- 任务号 `IMP-20260810-0001/0002`（编号服务正常）。

### 7. 导出（全链路 PASS）✅
- `POST /import-export/export`（moduleCode=materials, sort=code asc, pageSize=0 全量）→ 201 `status=PROCESSING` → 轮询 → `DONE` → 下载文件（xlsx）。
- **内容核对**：表头与模板一致；6 行数据含导入的 2 条；`code` 升序与请求 sort 一致 ✅（filter/sort/pageSize 生效）。

### 8. 幂等 ✅
- 同一 `Idempotency-Key` 重复 POST /sources：第一次 201 入库，第二次 **201 返回首次结果（同 id）**，库中仅 1 条 ✅。
- 备注：同 Key 携带**不同 body** 重放返回首次结果（中间件"响应与业务同事务"近似实现，Q-03 已知项，符合预期）。

### 9. 权限 ✅（读端点见 Q2）
- OPERATOR 写物料 → **403 FORBIDDEN** ✅；前端按 code 提示（见 Q1 对提示文案的影响）。
- OPERATOR 读用户列表/物料列表 → 200（后端读端点未加模块权限保护，**记 Q2 待裁决**）。

## 三、问题清单

### 待裁决（后端/契约侧）

| # | 严重度 | 主题 | 证据 |
|---|---|---|---|
| **Q1** | 高 | **错误响应 envelope 键名不一致**：controller/service 出口错误为 PascalCase `{"Code":...,"Message":...,"Data":null}`（401 登录失败、409 重复码、422 导入拒绝等），而 filter/中间件出口为 camelCase `{"code":...}`（403）；契约 2.1 要求小写 `code`。前端 `client.ts` 只认小写 → **PascalCase 错误的 code 全部退化为 UNKNOWN_ERROR**，按 code 提示/静默语义失效（如重复码 409 前端 toast 文案丢失） | `POST /auth/login` 错密码 → `{"Code":"LOGIN_FAILED",...}`；`POST /users` 重复 → `{"Code":"USERNAME_DUPLICATED",...}`；403 → `{"code":"FORBIDDEN",...}`（两套并存） |
| **Q2** | 中 | **读端点无模块路由权限保护**：`GET /api/users`、`POST /{resource}/search`、`GET ?keyword=`、`GET {id}` 仅 `[Authorize]`，无 `route.<moduleCode>` 检查；OPERATOR（无 route.master-data）可读物料列表/用户列表。契约"三级权限·路由=能否进入模块"是否要求 API 层强制，待裁决 | wang02(OPERATOR)：`GET /api/users`→200；`POST /api/materials/search`→200；写端点→403 |
| **Q3** | 中 | **仓库/库位/来源操作权限码缺失/误用**：后端权限注册表（`/api/permissions` 13 个码）**无 action.warehouse.\*/location.\*/source.\***；且 Warehouses/Sources/Locations 写端点 `[RequirePermission]` 误用 `action.material.create/edit/delete`（删除来源需"删除物料"权限）。待裁决：补注册表 or 本期不加操作点 | 控制器代码 `RequirePermission("action.material.*")`；`GET /api/permissions` 无 warehouse/location/source 码 |
| **Q4** | 低 | `GET /api/meta/fields/users` 未实现（认证权限.md 声明该端点）→ 404 | `GET /api/meta/fields/users` → 404 NOT_FOUND |
| **Q5** | 低 | `GET /api/roles` 响应为数组（契约未写明分页形态；前端本批无角色页，不阻塞） | `GET /api/roles` → data 为数组（3 项） |
| **Q6** | 中 | **JWT 密钥双来源不一致**：Program.cs 仅把 `AWMS_JWT_SECRET` 用于 JwtBearer；`IOptions<JwtOptions>`（TokenService 用）绑定 `Jwt:SecretKey`（appsettings 占位空）→ 未注入 `Jwt__SecretKey` 时登录即 500 `IDX10703: key length is zero`。联调以注入 `Jwt__SecretKey` 绕过（未改后端代码） | 后端日志 `SymmetricSecurityKey..ctor → IDX10703`；注入后正常 |

### 前端适配修复（联调期已在 `feat/frontend-3-integration` 完成，供协调者确认）

| # | 主题 | 说明 |
|---|---|---|
| F1 | 权限码/模块码对齐 | 前端注册表与 mock 原用 `route.master`/`menu.materials`/`action.material.update|import|export`；真实后端注册表为 `route.master-data`/`menu.master-data`/`action.material.edit`/`action.import|export`。已同步改 `route-registry.tsx`、`MaterialsPage.tsx`、`mocks/seed.ts` |
| F2 | 菜单 path 适配 | 后端菜单 path 为模块级（`/`、`/inbound`、`/master-data`、`/system`），前端路由为页面级（`/web/...`）。新增 `menuTarget()` 映射（模块→模块首页；已含 /web 原样）；登录落地改为"第一个可导航菜单"，修复**登录后跳 `/` 404** |
| F3 | 仓库/库位/来源按钮权限 | 后端无对应操作码（Q3），页面暂不传 permission 参数（`MasterListPage` 语义：undefined=默认允许）；Q3 裁决补码后接回 |
| F4 | i18n 补 key | `zh.ts`/`en.ts` 补 `nav.workspace/inbound/master-data/system`、`nav.group.workspace/operations/settings`、`field.*`（后端元数据 labelKey 全集）——修复侧边栏/筛选区显示 key 原文 |
| F5 | 新建/编辑表单残留 | 4 个 FormDialog 的 RHF `values` useMemo 依赖缺 `open` → 二次打开新建残留上次输入；依赖补 `open` 修复 |
| F6 | vite proxy | `vite.config.ts` 增 `/api → localhost:5000`（联调必需；生产构建不受影响） |

### 联调环境/工具说明（非缺陷）
- PG 端口 5433（本机 5432 被用户私有容器占用）；后端连接串已按 5433。
- Windows git-bash 下 curl 发送中文 body 编码损坏（GBK/UTF-8 混乱）→ 后端解析失败 400；**改用 python urllib（UTF-8）后全部正常**。属测试工具问题，非后端缺陷（浏览器 fetch 无此问题）。

## 四、遗留/未验证项（如实）

1. **引用删除保护**（`MATERIAL_IN_USE`/`WAREHOUSE_IN_USE`/`SOURCE_IN_USE`）：本期无引用数据源（批次由收货生成，第 4 批），删除保护路径未实测；后端集成测试已覆盖（122 用例绿）。
2. **库位"有货"删除保护**：依赖第 5 批库存表（指令已知提示，本期无引用即放行）。
3. **浏览器端 401 单飞刷新全链路**：后端 refresh 端点实测通过 + 前端单测覆盖；浏览器"token 过期→自动换新→原请求重放"未实测（可用手工过期 JWT 注入复现，留待抽查）。
4. **侧边栏页面级导航**：后端菜单为模块级（4 项），批次/仓库/库位/来源页面**无菜单入口，仅 URL 直达**（如 `/web/master/batches`）；待裁决：菜单粒度（模块→页面）设计如何演进（见 Q7 建议）。
5. searchCode 唯一性契约未写明（后端有唯一约束）——建议契约物料.md/仓库库位.md/来源.md 补充。
6. 联调测试数据保留在库（SMOKE-*/SMK2-*/IMP-MAT-*/WEB-MAT-001、用户 smoke02/wang02），供抽查；如需清理可执行 `DELETE` 或重建容器。

## 五、验证结论

- **接口层冒烟**：45 项断言（43 PASS + 2 项 Q2 记录）；导入导出全链路 16/16；refresh 5/5。
- **前端**：`pnpm test` 57/57 全绿（含适配修复后的守门用例）、`pnpm build` ✓、`pnpm lint` 0 errors。
- **浏览器实测**：登录落地→物料 CRUD（新建中文/重复 409/编辑 code 只读/删除）→筛选 DSL→批次/仓库/库位（嵌套）/来源页→导入导出弹窗→登出，全部正常；控制台零 JS 错误。
- 阻塞项：**Q1（错误 envelope 大小写）与 Q2（读端点权限）建议优先裁决**；Q1 修复后前端"按 code 提示"才能完整生效。
