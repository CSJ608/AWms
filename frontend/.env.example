# 前端环境变量说明（复制为 .env.development 可覆盖默认值）
#
# VITE_USE_MOCK: mock 数据层开关（MSW 拦截 /api/*，严格按契约 DTO）
#   - 不设置 / 非 'false'：开发模式默认启用 mock（后端未就绪时可独立开发自测）
#   - 'false'：切真实后端（联调时设置，需后端跑在 /api 同源或配置代理）
# 生产构建（pnpm build）不启用 MSW worker，天然连真实后端。
VITE_USE_MOCK=true
