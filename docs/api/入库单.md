# 入库单接口契约（第 4 批修订草案 v0.5）

> 状态：**第 4 批修订草案 v0.5（2026-08-20，待前后端复核）**。已回写双向开工评审与用户裁决；复核无异议后重新锁定。
> 依据：通用规范 v2.1、数据模型修订草案 v0.6、ADR-003。

## 一、生命周期与规则
- 创建即 CONFIRMED；无 DRAFT；不提供行修改（改数量=作废重建）。
- 作废：CONFIRMED/RECEIVING 可作废，留痕；作废后不可收货。
- 数量一致性策略由类型决定：PO=STRICT；PR/OT=LENIENT。
- **来源主数据**：PO 必须引用启用的 SUPPLIER；PR 必须引用启用的 WORKSHOP；OT 来源可空，填写时必须引用启用的 Source。`sourceType/sourceCode` 必须同时为空或同时有值。
- **唯一码**：创建接口不收 uniqueCodes——由打印端点（POST /api/print/unique-labels）按行生成并登记数量（打印契约 v0.4）。
- **lineNo**：后端分配（同一 order 内 1..N 自增），前端不传。
- **RECEIVED 推进（方案 A，同事务）**：收货提交事务内检查所有行 SUM(actualQty) >= expectedQty（严格=每行收齐；宽松=已覆盖）→ 满足即推进；不采用异步任务。

## 二、状态机
```
创建 ──► CONFIRMED ──► RECEIVING ──► RECEIVED（终态，同事务推进）
              │            │
              └──► VOIDED ─┘（作废留痕）
```
部分收货：一张入库单可被多张收货单分批引用；严格行一次收齐、不同行可分次；宽松行可拆、diff 累计。

## 三、端点
| 方法 | 路径 | 说明 |
|---|---|---|
| POST | /api/inbound-orders | 新建入库单（幂等） |
| POST | /api/inbound-orders/search | 列表（分页+筛选+filter/sort） |
| GET | /api/inbound-orders/{id} | 详情（含行） |
| POST | /api/inbound-orders/{id}/void | 作废（留痕） |

## 四、DTO
```json
{ "id": "uuid", "orderNo": "PO-20260810-0001", "type": "PO",
  "warehouseId": "uuid", "warehouseCode": "WH-01",
  "sourceType": "SUPPLIER", "sourceCode": "SUP-001",
  "status": "CONFIRMED",
  "lines": [ { "id": "uuid", "lineNo": 1, "materialId": "uuid", "materialCode": "MAT-001", "materialName": "螺母 M6",
    "expectedQty": "200.0000", "receivedQty": "0.0000", "remainingQty": "200.0000",
    "uniqueCodes": [{ "code": "BOX-20260820-0001", "quantity": "10.0000", "status": "UNRECEIVED", "receivedAt": null }] } ],
  "createdAt": "...", "createdBy": "管理员",
  "voidedAt": null, "voidedBy": null, "voidReason": null
}
```
> 行的唯一码清单（UNRECEIVED/RECEIVED）由打印端点登记后，在详情接口 `lines[].uniqueCodes` 返回（只读展示）；每个码携带其代表数量。
> `receivedQty` 为该行全部有效收货行 actualQty 之和；`remainingQty=max(expectedQty-receivedQty, 0)`，宽松超收时保持 0。

### 4.1 字段可空性

| 字段 | 必填/可空 | 场景规则 |
|---|---|---|
| `sourceType/sourceCode` | 条件必填，可同时为 null | PO/PR 必填；OT 可空；两者必须成对 |
| `voidedAt/voidedBy/voidReason` | 可为 null | 仅 VOIDED 有值 |
| `lines[].uniqueCodes` | 必填数组 | 非唯一码物料或尚未生成时为空数组 |
| `uniqueCodes[].receivedAt` | 可为 null | UNRECEIVED 为 null，RECEIVED 有值 |

## 五、端点详述
### POST /api/inbound-orders
- 请求：`{ type, warehouseId, sourceType?, sourceCode?, lines: [{ materialId, expectedQty }] }`（不收 uniqueCodes）。
- PO：`sourceType=SUPPLIER` 且来源必须存在并启用；PR：`sourceType=WORKSHOP` 且来源必须存在并启用；OT：来源可空，填写时同样必须存在并启用。
- 响应 201。错误：VALIDATION_ERROR、MATERIAL_NOT_FOUND、WAREHOUSE_NOT_FOUND、SOURCE_NOT_FOUND、ORDER_NUMBER_DUPLICATED、NUMBER_EXHAUSTED、UNAUTHORIZED/FORBIDDEN。
### POST /api/inbound-orders/search
- 请求：`{ type?, warehouseId?, status?, orderNo?, filter?, sort?, page?, pageSize? }`。
- 支持 filter DSL；**字段白名单**：orderNo、type、warehouseId、sourceType、sourceCode、status、createdAt、updatedAt。
- **排序白名单**：orderNo、type、status、createdAt；默认 createdAt desc。
### GET /api/inbound-orders/{id}
- 响应 200：InboundOrder（含行与唯一码清单）。错误：ORDER_NOT_FOUND（404）。
### POST /api/inbound-orders/{id}/void
- 请求：{ reason }。响应 200：status=VOIDED。错误：ORDER_NOT_FOUND、ORDER_STATUS_INVALID。

## 六、错误码
VALIDATION_ERROR、MATERIAL_NOT_FOUND、WAREHOUSE_NOT_FOUND、SOURCE_NOT_FOUND、ORDER_NUMBER_DUPLICATED、ORDER_NOT_FOUND、ORDER_STATUS_INVALID、ORDER_VOIDED、NUMBER_EXHAUSTED、UNAUTHORIZED、FORBIDDEN。

## 七、修订记录
| 日期 | 变更 |
|---|---|
| 2026-08-10 | v0.1 初始 |
| 2026-08-10 | v0.2 状态机与部分收货 |
| 2026-08-10 | v0.3 不收 uniqueCodes；filter/sort 白名单；lineNo；RECEIVED 同事务推进 |
| 2026-08-19 | v0.4 修订草案：列表对齐 POST /search；详情行补 receivedQty/remainingQty/uniqueCodes |
| 2026-08-20 | v0.5 修订草案：明确 PO/PR/OT 来源主数据规则、DTO 可空性和唯一码数量回显，待双方复核 |
