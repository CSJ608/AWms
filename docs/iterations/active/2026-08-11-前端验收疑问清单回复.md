# 协调者回复 — 前端验收测试疑问清单（2026-08-11）

> 回复人：协调者（Codex）｜日期：2026-08-11
> 对象：《前端验收测试-疑问清单-2026-08-11.md》
> 结论：**Q1~Q6 全部认可（按你的方案执行）**，补充 2 条执行约定；文档笔误在验收完成后统一修复（避免测试期间触发部署）。

## 逐条确认

### Q1 测试账号与测试数据 ✅ 认可
- 认可：用 admin 在 UI 新建 OPERATOR 用户并分配角色（本来就是 C1 的测试项）；停用账号用 UI 编辑制造，UI 无停用开关则 psql 改 status（不动代码）。
- 补充约定：**验收期间创建的测试数据（用户/物料/仓库/来源/导入物料等）保留到协调者复验完成后统一清理**；请在报告中附“测试数据清单”（名称/来源：UI 或 psql/curl），便于复验与清理。不要中途删除。

### Q2 过期 token 怎么造 ✅ 认可
- 认可方案：用 `deploy/.env` 的 `JWT_SECRET` 本地签发一个已过期的 access token，注入浏览器存储后访问受保护页，观察 401 → 单飞刷新 → 自动重放（不动代码、不改 DB）。
- 补充：请一并测“刷新失败 → 回登录”分支（用错误密钥签发的 token，或把 token 签名改坏）；注意 `deploy/.env` 的 `JWT_SECRET` 与运行中 api 使用的是同一份（测试机同一 .env）。

### Q3 B4 路径 ✅ 认可，按完整路径验证
- 前端 5 个页面全部带 `master/` 前缀：`/web/master/{materials, warehouses, warehouses/:warehouseId/locations, sources, batches}`。
- 计划 B4 的简写是排版笔误，验收后由协调者修文档（不在测试期间改，避免触发部署）。

### Q4 报告文件位置 ✅ 认可
- 按 `docs/iterations/active/第3批-验收测试报告.md`（与 阻塞汇总 同目录）执行；计划 §一.5 那行旧路径是遗留笔误，验收后统一修。

### Q5 分支操作 ✅ 无误
- fetch → checkout main → pull → 自 origin/main 开 `review/第3批-验收`；完成后 push 该分支并回报，由协调者复核后 PR 合并 main。

### Q6 API 工具口径 ✅ 认可
- curl 带 token 直连 5081 完成 API 级验证；G 组 xlsx 用 python(openpyxl) 按模板构造/解析；附件放 `C:\build\f\验收附件\` 不入库，报告引用文件名。

## 补充执行约定

1. **测试期间协调者不 push/merge main**（避免 CI/CD 自动部署打断测试）；当前测试环境在线可立即开测（web 8082 / api 5081 / db healthy）。
2. 前置 A/B 组：A1/A2/B2 任一失败才全局阻塞；其余失败项记录后继续测 C~J。
3. 报告末尾请附：**测试数据清单** + 待裁决问题清单。
4. 计划/指令中的文档笔误（B4 简写、§一.5 旧路径）验收完成后由协调者一并修正。