# 第3批-验收测试报告（测试环境联调迭代）

> 执行人：前端（hermes）｜日期：2026-08-11
> 依据：《第3批-验收测试计划.md》+《前端指令 — 第3批验收测试（测试环境联调迭代）》
> 环境：web http://localhost:8082 / api http://localhost:5081 / db 5434（docker awms-test-db-1）
> 账号：admin / Admin@2026Dev（测试专用）
> 状态：✅ 通过 / ❌ 失败（附初步原因）/ ⛔ 阻塞（前置不满足）/ ⚠️ 通过但有观察项
> 结论：**前置冒烟 A/B 组全部通过，无全局阻塞；C~J 全部执行完毕**。失败项 1（C4① 菜单权限过滤，归后端），观察项/问题若干（见问题清单）。

---

## 一、目标清单逐条结果

### A. 登录与会话（前置）

| # | 目标 | 状态 | 证据 / 说明 |
|---|---|---|---|
| A1 | 访问 / → 跳 /login，登录页正常渲染 | ✅ | http://localhost:8082/ → 301 跳转 /login；登录表单正常；控制台 0 错误 |
| A2 | admin 登录 → 进入后台（落地页非 404） | ✅ | admin 登录 → 落地 /web/master/materials（首个可导航菜单=物料页），列表正常渲染 |
| A3 | 错误密码 → 提示 LOGIN_FAILED（非 UNKNOWN_ERROR） | ✅ | UI toast「用户名或密码错误」；API `{"code":"LOGIN_FAILED","message":"用户名或密码错误"}` |
| A4 | 停用账号登录 → 提示 USER_DISABLED | ✅ | 自建测试账号 tst-disabled（API 创建，status=DISABLED）登录 → UI toast「账号已停用」；API `{"code":"USER_DISABLED"}`。创建手段见测试数据清单 |
| A5 | 登出 → 清会话回 /login | ✅ | 用户菜单 → 退出登录 → /login，localStorage 会话已清 |
| A6 | 刷新页面会话保持（/auth/me 恢复） | ✅ | /web/master/materials 等页面多次全量刷新，会话保持、无 404、无回登录 |
| A7 | 过期 token → 401 单飞刷新自动换新重放（浏览器全链路） | ✅ + 问题 F-01 | 用 deploy/.env JWT_SECRET 本地签发过期 token（exp=-1h，有效签名）注入 localStorage → 刷新 → /auth/me 401 → 自动 refresh → 新 token（1055 字符、exp+2h）→ 重放成功，页面正常、会话保持。**刷新失败分支**：坏签名 token → /auth/me 401 → refresh 401 → 会话清理回 /login ✅。⚠️ 过程中实测 /auth/refresh 被调用 3 次（预期 1 次/并发共享），见 F-01 |
| A8 | 未登录访问 /web/* → 重定向 /login | ✅ | 未登录访问 /web/master/materials → /login |

### B. 路由与导航（重点，用户反馈路由跳转问题）

| # | 目标 | 状态 | 证据 / 说明 |
|---|---|---|---|
| B1 | 根路径 /：未登录 → /login；已登录 → 落地 | ✅ + 待裁决② | 未登录 → /login ✅。已登录访问 / → **工作台占位页**（「模块开发中，敬请期待」），非 404 非回跳——与 2026-08-11 占位页修复（aaf0fa6）一致；与计划 B1「已登录 → /web」表述不符，列入待裁决② |
| B2 | 登录后落地页正确（首个可导航菜单） | ✅ | /web/master/materials（基础数据→物料，菜单 sort 顺序首个可导航项） |
| B3 | 侧边栏菜单逐一点击；未实现模块 → 占位页（非 404 非回跳） | ✅ + 问题 F-02 | 4 菜单逐个点击：工作台→ /（占位页）、入库→ /inbound（占位页）、系统管理→ /system（占位页）、基础数据→ /web/master/materials（物料页）。全部非 404 非回跳。⚠️ 占位页无侧边栏导航（见 F-02） |
| B4 | URL 直达 5 个主数据页面 | ✅ | /web/master/materials、/web/master/warehouses、/web/master/warehouses/019fef34-…/locations、/web/master/sources、/web/master/batches 全部可达、正常渲染（注：计划 B4 简写缺 master/ 前缀，按路由注册表完整路径验证，文档笔误由协调者验收后修） |
| B5 | 未知路径 → 应用内 404 页（文案正常） | ✅ | /web/master/nonexistent-page → 「404 / 您访问的页面不存在或已被移除」 |
| B6 | 任意已登录页面刷新不 404（SPA fallback） | ✅ | 物料/仓库/库位/来源/批次/占位页均全量刷新验证通过 |
| B7 | 浏览器前进/后退正常 | ✅ | 404 页后退 → 批次页；前进 → 404 页，均正常 |

### C. 用户/角色/权限（admin）

| # | 目标 | 状态 | 证据 / 说明 |
|---|---|---|---|
| C1 | 用户列表 keyword/分页；新建（重复 409）；编辑；分配角色；重置密码后新密码可登录 | ✅（API 层） | 全链路 API 验证通过（详见 c1_c2_api_test.py 输出）：列表 keyword+分页 200；新建 201；重复用户名 409 USERNAME_DUPLICATED「用户名已存在」；编辑 200；分配角色 200（roles=[SUPERVISOR]）；重置密码 204 → 新密码登录 200、旧密码 401 LOGIN_FAILED。**⛔ UI 层**：系统管理（/system）为占位页，前端无用户管理页面，C1 仅 API 层验证 → 待裁决③ |
| C2 | 角色：列表/新建（重复码 409）/分配权限/删除被引用角色 409 | ✅（API 层） | 列表 200（OPERATOR/SUPERVISOR/SYSTEM_ADMIN）；新建 201；重复码 409 ROLE_CODE_DUPLICATED；分配权限 200；被引用删除 409 ROLE_IN_USE「角色被用户引用，禁止删除」；解除引用后删除 204。**UI 层同 C1 缺失** → 待裁决③ |
| C3 | 权限列表：22 项（含 action.warehouse/location/source.* 9 码） | ✅ | GET /api/permissions → 22 项，含 action.warehouse.create/edit/delete、action.location.*、action.source.* 共 9 码，与种子一致 |
| C4 | OPERATOR：无主数据菜单；直达 /web/master/* → 无权限页；写物料 API → 403；页面按钮不显示 | ❌（①）/ ✅（②③④） | ① **菜单未过滤**：tst-operator 登录后侧边栏仍显示 基础数据/系统管理 等全部 4 项菜单 → 归后端（见问题清单 C4①，初步原因：MenuDefinition 种子未设 RequiredPermissionCode，BuildMenuTree 过滤未生效）。② 直达 /web/master/materials → 「无权限 / 您没有访问该页面的权限」✅。③ POST /api/materials → 403 FORBIDDEN「缺少操作权限：route.master-data」✅；补充验证：仅含 route.master-data 权限的用户 → 403「缺少操作权限：action.material.create」→ 后端 route+action 双级校验均生效 ✅。④ 无权限页不渲染页面内容，按钮不显示（随②成立）✅ |

### D. 主数据 CRUD

| # | 目标 | 状态 | 证据 / 说明 |
|---|---|---|---|
| D1 | 物料：新建/重复码 409/编辑（code 只读）/删除（未引用 204） | ✅ | UI 全链路：新建（中文名）→ 列表出现；重复码 → toast「物料编码已存在」（MATERIAL_CODE_DUPLICATED）；编辑对话框编码输入框 disabled（code 只读）→ 改名保存 → 列表更新；删除（确认框）→ 列表清空。英文名验证：M-D1-02「Acme Bolt」创建成功（契约无独立英文字段，name 支持中英文） |
| D2 | 仓库：新建/重复 409/编辑/删除（有库位 409） | ✅ | UI：新建 WH-D2 → 出现；重复 → toast「仓库编码已存在」；编辑（code 只读 disabled）改名成功；有库位删除 → toast「仓库下有库位，禁止删除」（409）；删库位后删除成功 |
| D3 | 库位（嵌套页）：新建/仓内重复 409/编辑/删除 | ✅ | UI（/web/master/warehouses/:id/locations）：新建 L-D2-01 → 出现；仓内重复 → toast「库位编码在仓内已存在」；编辑（类型暂存→默认）保存成功；删除成功。嵌套页含「返回仓库列表」链接、所属仓库展示 |
| D4 | 来源：新建（type+code 唯一 409）/编辑/删除 | ✅ | UI：新建 SUPPLIER/WS-01 → 出现；同 type+code 重复 → toast「同类型来源编码已存在」；编辑（code 只读）成功；删除成功 |
| D5 | searchCode：keyword 命中 code/name/searchCode；重复 searchCode 允许 | ✅ | UI keyword 搜索：「M-D1」→ 3 条（code 命中）；「Acme」→ 1 条（name 命中）；「CSZS」→ 2 条（searchCode 命中）；重复 searchCode（M-D1-03/M-D1-04 同 CSZS）两个均可创建、可检索 ✅ |

### E. 列表查询平台

| # | 目标 | 状态 | 证据 / 说明 |
|---|---|---|---|
| E1 | 5 资源筛选区元数据驱动渲染（无 key 原文） | ✅ | 物料/仓库/库位/来源/批次筛选区均为中文 label（编码/名称/状态/类型…）+ 操作符下拉（contains/eq…），无 key 原文 |
| E2 | filter DSL：UI 常用操作符生效；白名单外字段 → 400 | ✅ | API：status eq DISABLED → 1 条（精确）；code contains M-PG-0 → 9 条；白名单外字段 → 400 VALIDATION_ERROR「Field 'hacker' is not allowed for filtering」；白名单外操作符 → 400「Operator 'regex' is not supported」 |
| E3 | 排序：点击列排序生效；翻页稳定（不重不漏） | ✅ | UI：点「编码」列头 → 降序（M-PG-25 在前）生效；第 1 页 20 条 + 第 2 页 8 条，两页编码集合**零重叠**（overlap=[]），不重不漏 |
| E4 | 分页：默认 20、翻页正确；pageSize=0 全量 | ✅ | UI：共 28 条 → 当前筛选 20 条，下一页 8 条。API：pageSize=0 → total=28 items=28 全量 |
| E5 | 引用选择器快捷搜索（GET ?keyword=&pageSize=） | ✅ + 待裁决⑤ | API：GET /api/materials?keyword=CSZS&pageSize=10 → 1 条（M-D1-03）。⚠️ M-D1-04 同为 CSZS 但 status=DISABLED 未返回——快捷搜索默认过滤停用？契约未明示，列入待裁决⑤ |

### F. 批次

| # | 目标 | 状态 | 证据 / 说明 |
|---|---|---|---|
| F1 | 批次页列表（空/有数据）默认 createdAt DESC；keyword 搜索 | ✅ | 空列表 ✅（初始 0 条）。psql 造 2 条（B-F1-01/02，不同 CreatedAt）→ 列表默认 **createdAt DESC**（B-F1-02 在前）✅；keyword「F1-01」→ 1 条 ✅。批次状态显示「可用」（ACTIVE 映射） |
| F2 | 批次详情（无可跳过并注明） | 跳过（注明） | 批次为只读列表（页面提示「批次由系统自动建立（收货时生成），只读」），行无详情入口，无详情路由 → 本批无可测，跳过 |
| F3 | 嵌套 /api/materials/{id}/batches/search：存在 200 / 不存在 404 | ✅ | 存在物料 id → 200（2 条批次）；不存在 id（全 0 UUID）→ 404 |

### G. 导入导出

| # | 目标 | 状态 | 证据 / 说明 |
|---|---|---|---|
| G1 | 模板下载（数据/字典/说明 三 Sheet） | ✅ | GET /api/import-export/templates/materials → 200 xlsx（8273B），openpyxl 解析：sheets=['数据','字典','说明']；数据 Sheet 表头 7 列含示例行；字典含标签类型/默认单位枚举；说明 5 条。UI「下载标准模板」按钮触发下载请求 ✅ |
| G2 | precheck：干净 canExecute=true；重复码（文件内/库中）→ 失败明细 inline ≤200 + 前端展示 + canExecute=false | ✅ | API：clean.xlsx（2 行新数据）→ canExecute=true, fail=0；dup.xlsx（文件内重复+库中重复）→ canExecute=false, fail=2，failures inline 精确到行号/列/原始值/错误码（rowNo=2 code=M-IMP-01 MATERIAL_CODE_DUPLICATED；rowNo=3 code=M-D1-02）。UI 上传 dup.xlsx → 展示「共 4 行：成功 1，失败 3」+ 失败明细表格（行号/字段/原始值/原因）+「存在失败项…全部校验通过才可执行」+ **执行导入按钮 disabled** ✅ |
| G3 | execute：干净任务 → DONE → 列表可查 + DB 核对 | ✅ | API：execute → status=DONE, success=2；DB 核对 M-IMP-01/02 已入库（psql 查询 2 行）；POST /search keyword=M-IMP → 2 条 ✅。UI：上传 clean2.xlsx → precheck 全绿 → 点「执行导入」→ toast「导入完成：成功 2 行」→ DB 核对 M-IMP-04/05 入库（4 行）✅ |
| G4 | 失败任务 execute → 422 拒绝 | ✅ | API：execute 失败任务 → 422 IMPORT_VALIDATION_FAILED「预校验未通过，无法执行导入」。UI：canExecute=false 时执行导入按钮 disabled（无法触发）✅ |
| G5 | 导出：创建 PROCESSING → 轮询 DONE → 下载 xlsx；内容与 filter/sort/pageSize 一致（与库核对）；pageSize=0 全量 | ✅ | API：全量导出（pageSize=0）→ 201 PROCESSING → 轮询 DONE total=30 → 下载 xlsx → 解析 30 行，与库核对一致（3+25+2=30，物料全量）✅；filter 导出（status=DISABLED）→ DONE total=1 → 文件 1 行 M-D1-04 ✅；导出表头 8 列（含状态列） |

### H. 幂等

| # | 目标 | 状态 | 证据 / 说明 |
|---|---|---|---|
| H1 | 同 Idempotency-Key 重复写 → 只入库一次、返回首次结果 | ✅ | POST /api/sources（同 Key「h1-test-key-20260811」×2）→ 两次均 201 且 id 相同（019fef4c-1b59-…）；DB 核对 Sources 仅 1 条（WS-IMP-01）✅ |

### I. 错误信封与提示

| # | 目标 | 状态 | 证据 / 说明 |
|---|---|---|---|
| I1 | 各错误（401/409/403/422/404）前端按 code 显示正确文案（非 UNKNOWN_ERROR） | ✅ | 401 → LOGIN_FAILED「用户名或密码错误」（A3）、USER_DISABLED「账号已停用」（A4）；409 → 物料/仓库/库位/来源/用户/角色重复 toast 均正确中文；403 → C4 无权限页 + API FORBIDDEN；422 → G4 IMPORT_VALIDATION_FAILED；404 → 应用内 404 页（B5）+ API 404（F3）。全程未见 UNKNOWN_ERROR |
| I2 | envelope 键名小写（API 抽查） | ✅ | 全部 API 响应均为 `{code, message, data}` 小写键（多次抽查含错误响应） |

### J. 平台能力

| # | 目标 | 状态 | 证据 / 说明 |
|---|---|---|---|
| J1 | 中/英切换：菜单与筛选区不显示 key 原文 | ✅ + 问题 F-03/F-04/F-05 | 中→英切换后菜单（Workspace/Inbound/Master Data/System）、筛选区（Code/Name/Status/contains…）、表头、按钮（New/Edit/Delete/Import/Export/Prev/Next）全部英文，无 key 原文 ✅；切回中文正常。⚠️ 3 处 i18n 问题见问题清单（F-03 批控布尔原文、F-04 导入状态 key 原文、F-05 服务端错误文案中英均中文） |
| J2 | 浏览器控制台无 JS 错误 | ✅ | 全程（登录/CRUD/导入导出/语言切换/刷新/404/权限页）browser console 0 error、0 warning |

---

## 二、问题清单（初步原因分析，只测只报不修改）

| 编号 | 归属 | 严重度 | 问题 | 初步原因 | 复现/证据 |
|---|---|---|---|---|---|
| F-01 | 前端 | 中 | A7 过期 token 单飞刷新链路可用，但**会话恢复后旧 token 回写 localStorage**，导致页面后续查询再次 401、再次刷新（实测 1 次过期恢复触发 3 次 /auth/refresh） | frontend/src/platform/auth/auth-context.tsx:43-50：/auth/me 成功后用 `cached.token`（恢复前捕获的旧 token）构建 next 并 `sessionStore.save(next)`，覆盖了 client 层 refresh 刚换新的 token → 后续请求携带旧 token 再次 401。单飞逻辑本身正确（refresh-queue.ts 并发共享），问题是串行触发 | 资源时间线：me(34ms)401 → refresh(47) → me(63)重放 → meta(104)/search(105) 再次 401 → refresh(125) → meta 重放(137) → refresh(179) → search 重放(191)。建议（供协调者裁决）：restore 时用 apiMe 响应中的 token/expiresAt（服务端 /auth/me 返回当前 token）或不再回写 |
| F-02 | 前端 | 低 | 占位页（/、/inbound、/system）**无侧边栏导航**，从菜单进入后成死胡同（只能浏览器后退） | App.tsx 占位路由直接渲染 ModulePlaceholderPage，未包 AppLayout | /inbound 页 body 无 aside/nav 元素（sidebarLinks=[]） |
| F-03 | 前端 | 低 | 物料列表「批控」列显示布尔原文 `false/true`（中英均如此，未映射是/否） | 列表单元格布尔值未做 i18n/展示映射 | 物料页批控列（M-D1-02=false、M-D1-03=true），中英态均原文 |
| F-04 | 前端 | 低 | 导入任务状态显示 **i18n key 原文** `enums.importTaskStatus.prechecked/done`（中英均泄漏） | 导入对话框状态字段直接用 key 渲染，zh/en 语言包均无该 key | 导入对话框「状态: enums.importTaskStatus.prechecked」（中英态均复现） |
| F-05 | 待裁决（后端/契约） | 低 | 服务端错误文案无 i18n：EN 界面下失败明细的 columnName「物料编码」、errorMsg「物料编码已存在」仍为中文 | 后端返回的 columnName/errorMsg 为中文硬编码，前端未翻译（或契约未定义错误文案 i18n 机制） | EN 态导入 dup.xlsx 失败明细（Row/Column/Raw Value/Reason 表头英文、内容中文） |
| C4① | 后端 | 高 | OPERATOR 登录后菜单未按权限过滤：侧边栏显示基础数据/系统管理等全部 4 项（计划要求无主数据菜单） | 后端 BuildMenuTree 已有过滤逻辑，但 **MenuDefinition 种子（AWmsDbContext.cs:350-354）未设置 RequiredPermissionCode**，过滤条件恒不成立 → 返回全量菜单。前端按契约（mock seed.ts 注释「后端按角色过滤后返回，前端只渲染」）不自行过滤 | tst-operator /auth/me：permissions=['route.inbound','menu.inbound','action.receiving.create']，menus.web 却含 menu.master-data/menu.system 等 4 项 |

## 三、待裁决问题清单

1. **未实现模块的菜单入口应如何表现**（占位页/隐藏/禁用）——指令 §四 指定记录。现状：占位页方案已上线且非 404 非回跳（B3 ✅）；附带 F-02（占位页无导航）一并裁决。
2. **B1 行为与计划描述差异**：已登录访问 `/` → 工作台占位页（与 2026-08-11 占位页修复一致），计划 B1「已登录 → /web」表述不符——以修复后行为为准还是计划为准？（文档笔误由协调者验收后统一修，本报告不修改任何文档）
3. **系统管理页面范围**：用户/角色/权限无 UI 页面（/system 为占位页），C1~C3 仅 API 层验证通过——系统管理（用户/角色/权限）UI 是否列入后续批次？本批 C 组按「API 全通 + UI 缺失」记录。
4. **F-05 服务端错误文案 i18n 归属**：错误 message/columnName 的国际化由后端（契约层）还是前端（翻译层）负责？
5. **E5 快捷搜索默认过滤停用**：GET keyword 快捷搜索不返回 DISABLED 物料（M-D1-04），契约未明示默认过滤规则——是有意设计还是缺陷？
6. **新建对话框表单值复用**：新建物料对话框关闭再开后保留上次输入值（创建表单不清空）——有意（草稿保留）还是缺陷？（UX 观察）

## 四、测试数据清单（保留至协调者复验，复验后统一清理）

| 类型 | 数据 | 来源 | 说明 |
|---|---|---|---|
| 用户 | tst-disabled / Test@2026Dev | API POST /api/users | 状态 DISABLED，A4 用 |
| 用户 | tst-operator / Test@2026Dev | API POST /api/users | 角色 OPERATOR，C4 用 |
| 用户 | tst-c1 / NewPass@2026 | API POST /api/users | 角色 SUPERVISOR，C1e 重置密码测试后新密码生效 |
| 用户 | tst-routeonly / Test@2026Dev | API POST /api/users | 角色 ROUTE_ONLY（仅 route.master-data），C4 补充验证用 |
| 角色 | ROUTE_ONLY | API POST /api/roles | 权限 [route.master-data]，保留（QC_LEAD 已按 C2d 删除） |
| 仓库 | WH-B4（B4验收仓库） | API POST /api/warehouses | B4/D2 用，保留（WH-D2 已按 D2/D3 删除） |
| 物料 | M-D1-02 Acme Bolt（searchCode AB）、M-D1-03 测试助记码物料（CSZS）、M-D1-04 重复助记码物料（CSZS，**DISABLED**） | API POST /api/materials | D1/D5/E2/E5/F 用 |
| 物料 | M-PG-01 ~ M-PG-25（分页测试物料） | API POST /api/materials（脚本） | E3/E4 分页排序用 |
| 物料 | M-IMP-01/02（导入测试物料） | API 导入 execute（clean.xlsx） | G2/G3 用，DB 已核对 |
| 物料 | M-IMP-04/05（UI导入测试） | UI 导入 execute（clean2.xlsx） | G3 UI 用，DB 已核对 |
| 来源 | WS-IMP-01（幂等测试来源） | API POST /api/sources（Idempotency-Key） | H1 用，DB 1 条 |
| 批次 | B-F1-01 / B-F1-02（物料 M-D1-03） | psql 直插 Batches | F1/F3 用（CreatedAt 2026-08-10/11） |
| 导入任务 | IMP-20260811-0001/0005/0006/0007 | 导入流程 | G2/G3/G4 留痕 |
| 导出任务 | 全量导出（30 行）、filter 导出（1 行） | 导出流程 | G5 留痕，文件在本地附件目录 |

> 已删除（流程验证后清理）：M-D1-01（D1 删除）、WH-D2 及 L-D2-01（D2/D3）、WS-01（D4）、QC_LEAD 角色（C2d）。
> 附件（不入库）：`验收附件/`（api-body-*.json、*_test.py、make_tokens.py、token_server.py、export_all.xlsx、export_disabled.xlsx 等）+ 模板/测试文件（本地 Temp）。

## 五、执行说明

- 全程未修改任何代码/契约/文档（只测只报）；问题仅记录初步原因，修复由协调者裁决。
- A7 过期 token 方案：deploy/.env JWT_SECRET 本地签发过期/坏签名 token 注入浏览器（不动代码不改 DB），并补测「刷新失败→回登录」分支（协调者 Q2 补充约定）。
- 计划 B4 简写、§一.5 旧路径等文档笔误未改动，按前端指令在 `docs/iterations/active/` 出具本报告（协调者验收后统一修文档）。
- 截图说明：本环境无截图工具，证据以 URL/请求响应/DOM 快照/DB 查询为主；关键页面（登录、占位页、无权限页、404、导入对话框等）DOM 文本已在对应条目列出，可按需人工复核。
