> 状态：已归档（2026-08-22）——仅供追溯，不作为当前实现依据

# 第 4 批用户验收与发布记录

> 验收日期：2026-08-22
> 验收方式：用户基于测试环境与真实 UI 联调报告验收
> 验收结论：**通过**
> 发布结论：**已合并 main，并成功部署测试环境**

## 一、验收范围

- Web 入库单创建、详情、打印、收货记录与异常处理。
- PDA 收货、质检、异常上报、上架。
- 真实 Web/API/PostgreSQL/附件存储联调。
- PDA 预建单 `orderLineId` 映射 P0 修复复测。
- `/pda` 主菜单 `pda.*` 权限映射 P1 修复复测。

## 二、复测结论

- 修复复测通过。
- PDA 收货、数量拦截、照片上传、质检 PASS、上架回归均通过。
- 最终状态：
  - Receipt：`DONE`
  - Receipt line：`PUTAWAY_DONE`
  - PO：`RECEIVED`

完整场景、测试数据、DB 复核与截图证据见：[2026-08-21-第4批-真实UI联调报告.md](2026-08-21-第4批-真实UI联调报告.md)。

## 三、关键证据

| 证据 | 值 / 链接 |
|---|---|
| 修复复测提交 | `ddfcf80668e486ee302c1a32e3ec40fd05a8e25c` |
| 修复分支 CI | https://github.com/CSJ608/AWms/actions/runs/32493982333 |
| Integration HEAD | `91308f5aa47be8ee1ae93ad4c0427bfffcd88388` |
| Integration PR CI | https://github.com/CSJ608/AWms/actions/runs/32502192768 |
| Integration -> Main PR | https://github.com/CSJ608/AWms/pull/13 |
| Main 合并提交 | `81d534f072e35a03083fc9d2fe25503fba956517` |
| Main CI/CD 与部署 | https://github.com/CSJ608/AWms/actions/runs/32569822422 |

## 四、发布记录

1. PR #13 前后端 CI 通过，用户明确验收通过。
2. PR #13 以 merge commit 方式合并 `main`，合并提交为 `81d534f072e35a03083fc9d2fe25503fba956517`。
3. Main 首次部署在 self-hosted runner checkout 阶段因访问 `github.com:443` 超时失败；用户开启 VPN 后重跑成功。
4. Main 后端构建测试、前端 lint/build/test、测试环境部署全部通过。
5. 测试环境：
   - Web：`http://localhost:8082`
   - API：`http://localhost:5081`
   - PostgreSQL：localhost `5434`
6. 部署后探测：Web 返回 `200`；API 实际路由可达。项目当前无 `/health` 端点。

## 五、非阻塞观察与后续输入

以下问题不阻塞第 4 批验收，已由用户确认放到后续迭代处理：

1. **生产/失效日期未落库**：UI 填写的生产日期、失效日期在 API 与 DB 中为 `null`；来源批号正确落库，主链状态正确。后续需复核前端状态、请求体与后端 DTO 映射，并补充契约测试和端到端断言。
2. **PC 页面结构需要重构**：
   - 顶部导航栏仅保留基础功能；LOGO 与产品名称左对齐，搜索、多语言、用户头像菜单右对齐。
   - 标签页栏固定“标签页 / 菜单”两个入口，其余为活动标签页。
   - 标签页状态提升为全局应用状态，切换菜单不得销毁已有活动标签页。
   - 内容栏承载当前活动标签页对应内容，最大化业务信息区域。
3. **多语言缺失**：入库管理等相关页面未完整接入多语言；需要在后续批次先定前后端与 i18n 规范，再补齐页面。
4. **开发规范需要复核**：随着内容增多，需分别讨论前端和后端基础规范，确保框架基础设施可扩展。
5. **工程卫生**：GitHub Actions Node 20 弃用提示、前端 Fast Refresh warning 可在后续批次评估处理。

## 六、归档裁定

- 第 4 批状态改为“已验收、已合并、已部署、已归档”。
- `docs/iterations/active/` 与 `docs/reviews/` 不再保留第 4 批过程文件。
- 本记录只作追溯，不作为后续实现的直接依据；后续需求应进入第 5 批 active 计划与对应标准文档。