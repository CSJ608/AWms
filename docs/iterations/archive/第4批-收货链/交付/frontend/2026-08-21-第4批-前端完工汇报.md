> 状态：已归档（2026-08-22）——仅供追溯，不作为当前实现依据

# 第4批“收货链”前端完工汇报

> 远端最终提交：`3501a6d70608d6f95631882a9eb53a6ece1fdb27`

## 完成范围

- 基于第三轮整改提交 `be4c35a`，同步契约责任人裁决后的 `docs/api/收货.md` v0.6 和新锁定记录；未修改后端业务代码、界面规格或前后端完工审核记录。
- 质检收到 `QC_STATUS_INVALID` 后完整重置异常模式、原因、备注和当前附件队列；旧任务附件立即移出可提交范围，自动删除未认领服务端附件。
- 自动删除失败时保留独立清理队列、原任务号、文件名、错误和稳定删除 key，可在选择其他质检任务后继续重试；任务 B 不会读取或提交任务 A 的附件 ID。
- 增加 A 上传照片、A 被并发处理、A 提交失效、附件清理失败、选择 B、B 草稿为空、清理重试成功、B 仅提交自身附件的完整回归测试。
- MSW 对入库单创建/作废、收货、回执、质检、异常 resolve、上架、附件、全部入库链打印生成和打印 retry 统一前置检查 `Idempotency-Key`；缺失时在修改状态前返回 `400 VALIDATION_ERROR / Idempotency-Key 必填`，同 key 重放逻辑保持不变。
- Linux CI 使用 lockfile 固定的 `playwright@1.62.1` 安装 Chromium 与系统依赖；Windows 浏览器测试继续使用 `CHROME_PATH` 或本机 Chrome 默认路径。
- `0eea516` 将质检、异常 resolve、上架三个 API 返回类型由 `unknown` 改为 `Receipt`；MSW 分别同步为 `200 + Receipt`、`200 + Receipt`、`201 + Receipt`，同 key 重放保留首次 status 与完整 Receipt envelope。
- 新增三个逐端点 MSW 响应契约测试，直接断言 HTTP status、`code/message/data`、Receipt 与 ReceiptLine 完整字段集合，并通过类型化 API 重放确认返回 `Receipt`。
- `78b16b0` 同步 CI 安全门禁修复：将 `Testcontainers.PostgreSql` 由 4.3.0 升至 4.14.0，传递依赖切换到修复版 `SSH.NET 2026.0.0`，并适配新版 PostgreSQL builder 构造函数。该改动仅因 workflow 在每个 feat 分支同时验证前后端，不影响前端运行时代码。

## 实现文件

- `.github/workflows/ci.yml`
- `frontend/package.json`
- `frontend/pnpm-lock.yaml`
- `frontend/src/mocks/inbound-handlers.ts`
- `frontend/src/mocks/inbound-handlers.test.ts`
- `frontend/src/api/index.ts`
- `frontend/src/modules/pda/PdaHomePage.tsx`
- `frontend/src/modules/pda/useAttachmentUploads.ts`
- `frontend/src/modules/pda/PdaInbound.test.tsx`
- `docs/reviews/2026-08-21-第4批-前端完工汇报.md`
- `docs/api/README.md`
- `docs/api/收货.md`
- `docs/reviews/2026-08-21-第4批-契约锁定记录.md`
- `backend/Directory.Packages.props`
- `backend/AWms.IntegrationTests/ApiTestFixture.cs`
- `backend/AWms.IntegrationTests/PostgreSqlFixture.cs`

## 实际测试结果

- `pnpm lint`：通过，退出码 0；保留既有 10 条 Fast Refresh warning。
- `pnpm build`：通过，退出码 0；PWA 产物生成，保留主 chunk 大于 500 kB 的既有提示。
- `pnpm test`：通过；jsdom/Vitest 16 个文件、120 项测试，Playwright/Chrome 1 个文件、1 项测试，失败 0。
- `pnpm test:browser`：通过；1 个文件、1 项真实浏览器测试，失败 0。
- 定点响应契约回归：`inbound-handlers.test.ts` 26/26 通过，其中新增质检、resolve、上架 3 项。
- Bearer 图片/PDF 预览与 object URL 释放测试继续包含在完整 jsdom 和浏览器测试中并通过。
- CI 精确后端预检：`dotnet build backend/AWms.slnx --configuration Release` 通过，0 warning / 0 error；`dotnet test backend/AWms.slnx --configuration Release --no-build` 通过，当前前端分支基线单元 96/96、PostgreSQL 集成 45/45。
- `git diff --check 80700f9..HEAD` 与最终 `git status --short` 结果在提交完成后复核并记录于交付回复。
- 远端最终提交 `3501a6d` 的 push CI（run `32458735306`）通过：后端 job 与前端 job 均为 success；PR #11 的跨端 CI（run `32459942078`）再次通过，随后合并至 `integration/第4批-收货链`。

## 契约偏差/需求回流

- 无契约偏差。
- 契约责任人已正式裁决并将 `docs/api/收货.md` 升至 v0.6：质检和异常 resolve 为 `200 + ApiResponse<Receipt>`，上架为 `201 + ApiResponse<Receipt>`。
- 前端 API 类型、MSW status/envelope/data 和逐端点测试已按 v0.6 同步；未新增字段、端点、错误码、权限或交互。
- CI 安全门禁修复只涉及后端测试基础设施依赖，因为同一 workflow 会在前端 feat 分支运行后端 job；不构成前端或业务契约偏差。

## 遗留问题

- 当前前端仍有既有 `react(only-export-components)` lint warning，不阻断本批门禁。
- 生产构建仍提示主包体超过 500 kB，可在后续性能批次处理路由级拆包。
- 原远端 `7fb7a9f` 的 workflow 曾因后端 NuGet 审计命中 `SSH.NET 2024.2.0` 高危漏洞而失败；该问题已在 `78b16b0` 同步修复。最终远端 `3501a6d` 的 push CI 和 PR #11 跨端 CI 均已全绿，安全门禁已关闭。
- 本次响应契约开放项已关闭，未发现新的前端或 MSW 阻塞问题。

## 实际耗时

- 首轮实现与前两轮整改累计约 1 小时 50 分钟；第三轮整改、回归与完整验证约 45 分钟；成功响应契约同步与验证新增约 17 分钟；CI 安全依赖同步和分支基线验证新增约 8 分钟，累计约 3 小时。
