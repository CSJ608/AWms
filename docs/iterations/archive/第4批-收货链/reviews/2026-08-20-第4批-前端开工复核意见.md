> 状态：已归档（2026-08-22）——仅供追溯，不作为当前实现依据

# 第4批“收货链”前端开工复核意见

> 日期：2026-08-20
> 分支：`review/第4批-收货链`
> 复核提交：`84c5c8a0e26e028a18d209852a7a45f77618fccc`
> 原评审基线：`0c8efc4`
> 复核方式：按要求执行 `git fetch origin` 与 `git rev-parse origin/review/第4批-收货链`；通过 `git diff 0c8efc4..84c5c8a` 与 `git show 84c5c8a:<文件路径>` 审阅远端提交，未切换分支。

## 结论

**无异议，可以锁定。**

本次“待前后端复核 / 修订草案”属于正常门禁状态，不作为异议。`84c5c8a` 已将原 FE-API-01～03、FE-UI-01 以及后端 P0 裁决回写到标准契约和界面规格正文；剩余 `/inbound/*` 路由守卫、PDA 菜单权限、MSW 数据与用例覆盖已经明确归入 F-01/F-02/F-06 实现任务，不再构成契约锁定阻塞。

## 原前端异议闭合情况

| 编号 | 复核结论 | 证据 |
|---|---|---|
| FE-API-01 单据码扫码上下文不足 | 已闭合 | `84c5c8a:docs/api/标签解析.md:39-48` 明确 `ScanResult.document` 返回 `inboundOrderId/docType/docNo/warehouseId/warehouseCode/status/lines[]`；`84c5c8a:docs/api/标签解析.md:85` 明确 `t=D` 返回完整单据与行列表；`84c5c8a:docs/design/screens/收货-PDA-规格-草案.md:113` 明确 PDA 直接使用解析结果打开预建单，不再按 `docNo` 二次搜索。 |
| FE-API-02 上架默认标签数量缺口 | 已闭合 | `84c5c8a:docs/api/收货.md:108-114` 在 `PutawayTodo` 增加 `defaultQtyPerLabel`；`84c5c8a:docs/api/收货.md:134` 明确可为 null，null 时前端默认整行 1 张；`84c5c8a:docs/design/screens/质检上架-PDA-规格-草案.md:58-59` 与界面行为一致。 |
| FE-API-03 DTO required/nullable 不清 | 已闭合 | `84c5c8a:docs/api/入库单.md:47-54` 明确 `InboundOrder` 可空/数组规则；`84c5c8a:docs/api/收货.md:67-80` 提供无预建单 PR 响应示例且声明可空字段实际返回 null、不省略；`84c5c8a:docs/api/收货.md:123-134` 明确 `Receipt/ReceiptLine/PutawayTodo` 场景可空性。足以生成本批 TypeScript 类型和 MSW mock。 |
| FE-UI-01 Web 打印参数交互不明 | 已闭合 | `84c5c8a:docs/design/screens/入库管理-Web-规格-草案.md:87-90` 明确外标签和唯一码标签均为单据行操作菜单触发、只作用于当前行；参数对话框包含 `count`、`qtyPerCode`、默认值、范围、登记总量、PO 阻断与 PR/OT 超收确认；`84c5c8a:docs/api/打印.md:53-66` 与接口参数一致。 |

## 指定复核项

1. **`t=D` 是否足以直接建立 PDA 收货上下文：足够。** 解析结果包含入库单 ID、仓库、状态和行列表；PDA 规格明确直接打开预建单，并在仓库不一致、作废、已收完等 warning 上阻断。
2. **DTO required/nullable 是否足以生成 TypeScript 类型和 Mock：足够。** 入库单、收货单、收货行、唯一码、上架待办均有字段表或响应示例；可空字段以 null 表达，数组不省略，场景条件清楚。
3. **`PutawayTodo.defaultQtyPerLabel` 是否明确：明确。** 来源为物料，可为 null；null 时按整行 1 张处理，界面和打印契约一致。
4. **Web 是否明确为单行打印操作加参数对话框：明确。** 本批不做多行勾选；外标签输入 `count`，唯一码输入 `count/qtyPerCode`，补打从历史 PrintJob/PDF 打开。
5. **批次标签一个候选直达、多个候选选择是否可实现：可实现。** `quality-todos/search` 与 `putaway-todos/search` 支持 `warehouseId/materialId/batchId` 查询；质检 PDA 规格明确 1 条直达、多条展示候选、0 条提示无匹配；上架同规则。
6. **路由守卫、PDA 菜单和 MSW 是否归入实现任务：已明确。** `84c5c8a:docs/iterations/active/迭代计划-第4批-收货链.md:63-68` 把 `/inbound`、PDA 入口、MSW 与测试列入 F-01/F-02/F-06；`84c5c8a:docs/iterations/active/迭代计划-第4批-收货链.md:129` 明确这些属于实现任务；`84c5c8a:docs/reviews/2026-08-20-第4批-开工评审汇总与决议.md:42-44` 给出落地要求。

## 其他交叉检查

- 打印闭环已从 `PrintLog` 改为 `PrintJob + PrintJobItem`，并补齐查询、详情、retry、文件状态和错误码；可支撑 Web PDF、PDA 预览和补打。
- 唯一码数量守恒已落到 `UniqueCode.quantity`、扫码解析 `uniqueCode.quantity`、收货提交数量校验和 `UNIQUE_CODE_QTY_MISMATCH`。
- 附件归属已明确：收货照片为 `RECEIPT/receiptId`，质检异常照片为 `EXCEPTION/qualityCheckId`，PASS 本批不接收照片。
- 权限契约已明确 Web `/inbound` 与 PDA `receiving/qc/putaway` 的显示条件，以及 8 个第 4 批操作权限。
- 数据模型已补关键唯一约束、索引与并发验收，能支撑后端写侧防重和库存守恒测试。

## 锁定建议

双方复核均无异议后，可由协调者仅做状态门禁更新，将第 4 批相关契约从“待复核/修订草案”统一标为锁定；不需要再调整契约语义。
