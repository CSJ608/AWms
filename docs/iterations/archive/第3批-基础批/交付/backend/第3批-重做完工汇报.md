# 第3批-重做完工汇报（任务化提交版 v2）

> 状态：已归档（2026-08-19）——仅供追溯，不作为当前实现依据
>
> 后端 AI（Codex）｜日期：2026-08-10
> 分支：`feat/backend-3-rework-2`（基于最新 main，commit acd3742 起）
> 依据：docs/reviews/2026-08-10-后端开工指令-第3批-重做（任务拆分版 v2）、docs/reviews/2026-08-10-后端重做复验意见-第3批.md、docs/guides/后端工程规范.md v1.3、docs/api/ 契约

## ① 完成范围

按任务化提交完成第3批基础批后端全部需求（B-01~B-08），核心用例全部真实测试、0 Skip：

| 需求 | 结果 | 说明 |
|---|---|---|
| B-01 工程骨架+迁移 | ✅ | slnx + 四层 + CPM/global.json/Directory.Build.props；EF Migrations（InitialCreate 16 表 + 索引齐备 §3.3）；启动即 MigrateAsync（无 EnsureCreated）；迁移可应用由 Testcontainers 集成测试实测 |
| B-02 认证权限 | ✅ | login/refresh(过期换新)/logout/me/users/roles/permissions 全套端点；LoginResponse 契约形状（token/expiresAt/user/permissions/menus.web+pda）；初始管理员（环境变量注入+首启重置+测试）；RequirePermission 落地到写端点/导入导出；logout 无状态；me 不重新签发 |
| B-03 编号服务 | ✅ | INSERT..ON CONFLICT..UPDATE..RETURNING 原子自增（Npgsql 命名参数）；BATCH 动态作用域；真实 PG 并发 50 取号无重复、耗尽抛 NUMBER_EXHAUSTED、格式（BATCH=260810001 / TXN_GROUP=15 位 / IMPORT_TASK=IMP-20260810-0001） |
| B-04 查询平台 | ✅ | filter DSL 接入 5 个 Search 服务 + 嵌套 batches/search；13 操作符真实现（contains/startsWith JsonElement、isNull 值类型、between DateOnly 上界+1 天、嵌套 and/or 合并）；白名单外字段/操作符/排序 → 400；排序唯一性兜底（主数据 id asc、用户/时间 id DESC）；真实 PG 翻译测试 |
| B-05 主数据 API | ✅ | 物料/仓库/库位/来源/批次 CRUD + searchCode/keyword + 嵌套 batches/search；重复码 409（物料/仓库/库位/来源）；删除引用保护（物料-批次、仓库-库位、来源-批次）；GET 快捷搜索（pageSize≤10）与详情直查（无 int.MaxValue 全量扫描） |
| B-06 导入 | ✅ | precheck 不落业务数据（任务留痕）+ failures 固定 inline ≤200；execute 同一事务重校验唯一性 + 真实入库（失败全回滚）；导入 E2E（multipart→precheck→execute→库中可查）不 Skip |
| B-07 导出 | ✅ | 请求体 filter/sort 透传 + pageSize 生效；状态 PROCESSING→DONE（枚举已补 PROCESSING）；后台任务用 IServiceScopeFactory 独立 DbContext 作用域（不捕获请求作用域）；E2E 轮询 + 文件下载 |
| B-08 幂等+测试 | ✅ | Idempotency-Key 接线全部写端点（全局过滤器）：同 key 返回首次结果（含错误响应）、并发同 key 仅执行一次、TTL 24h 过期可重放；单元 93 + 集成 29 全绿 0 Skip；覆盖率核心服务 79.1%（≥70%） |

## ② 文件清单

- 工程：backend/AWms.slnx、global.json、Directory.Build.props、Directory.Packages.props、NuGet.Config
- Domain：Entities/*（16 实体）、Enums/*、Interfaces/IServices.cs、Dtos/{Common,Auth,Users,Roles,Permissions,Meta,Materials,Warehouses,Sources,Batches,ImportExport}
- Infrastructure：Data/AWmsDbContext.cs、Data/AWmsDbContextFactory.cs、Migrations/20260810152301_InitialCreate*、Services/{AdminSeedService,DomainException,Argon2PasswordHasher,TokenService,AuthService,NumberingService,QueryService,MasterDataService,ImportExportService,IdempotencyService}.cs、ServiceCollectionExtensions.cs
- Api：Program.cs、appsettings*.json（密钥仅占位）、Controllers/*（Auth/Users/Roles/Permissions/Meta/Materials/Warehouses/Sources/Batches/ImportExport）、Middleware/{ExceptionHandler,RequirePermission,IdempotencyFilter}.cs
- 测试：AWms.Tests/Services/*（15 个测试类）、AWms.IntegrationTests/*（PostgreSqlFixture、ApiTestFixture、Numbering/Query/ImportExport 集成、Auth/MasterData/ImportExport/Idempotency API 测试）
- 交付：backend/交付/TASKS.md、问题清单.md、coverage/覆盖率报告.md + Cobertura XML

## ③ 测试结果

命令与用例数（2026-08-10 实测）：

```
dotnet build AWms.slnx --no-restore
# 0 错误 0 警告

dotnet test AWms.slnx --no-build --no-restore
# AWms.Tests        ：93 通过 / 0 失败 / 0 跳过
# AWms.IntegrationTests：29 通过 / 0 失败 / 0 跳过（Testcontainers PostgreSQL 16，真实迁移）
# 合计 122 用例全绿，0 Skip

dotnet test --collect:"XPlat Code Coverage"（单元+集成合并）
# 核心服务行覆盖 79.1%（≥70%）；全仓 81.6%（详见 backend/交付/coverage/覆盖率报告.md）
```

覆盖核心用例：迁移可应用（fixture 启动即应用）、登录成功/密码错/停用、refresh 用真实过期 token 换新、me 不重签发、权限 403、并发取号 50、耗尽、filter DSL 全操作符（单测+PG 翻译）、翻页/排序兜底、重复码 409、引用保护、导入 precheck/execute E2E、导出 PROCESSING→DONE E2E、幂等同 key/并发/错误/TTL。

## ④ 契约偏差与需求回流

- 未单方修改 docs/；发现的问题全部记入 backend/交付/问题清单.md（Q-01~Q-07），其中 **待裁决 2 项**：Q-01 批次 filter 白名单 materialName（实体无该列）；Q-02 枚举表未列 PROCESSING（实体已补，建议契约一致化）。
- 复验意见其余 P2 项均已闭环（见⑥复验清单）。

## ⑤ 遗留问题（如实，不标 ✅）

1. 异步导出为 Task.Run + 独立作用域（最小形态），未升级 IHostedService/持久队列；进程重启会丢未完成任务（Q-04，P3）。
2. 幂等“响应与业务同事务”为近似实现：预留先行杜绝重复执行；业务提交后写响应，极端崩溃窗口下重试返回 409 而非首次结果（Q-03，P3）。
3. 批次 materialName 筛选未实现（Q-01，待裁决）。
4. 库位删除的“有货/被引用”引用保护：库存表属第 5 批，本期仅无引用即放行（契约仓库库位 v0.3「库位有货/被引用禁止删除」中“有货”部分后置）。
5. Testcontainers 在本机关闭 Ryuk（Docker Hub 不可达），容器由 fixture 显式回收（Q-05，环境相关）。

## ⑥ 耗时

- 2026-08-10 单会话完成 T0~T10（文档研读 + 逐任务实现 + 测试 + 汇报）；未使用分时段统计，总工作量约 1 个工作日等效。

---

## 任务对照表（T0~T10 → commit / 目标 / 验收 / 测试）

| 任务 | commit | 目标 | 验收结果 | 测试命令与用例 |
|---|---|---|---|---|
| T0 | 1940202 docs / e460605 feat | 任务拆解 + 工程骨架 | ✅ slnx/四层/CPM/global.json/Directory.Build.props；TASKS.md | `dotnet build` 0 error 0 warning |
| T1 | f103a89 | 数据模型+种子+初始管理员+初始迁移 | ✅ 16 实体/枚举/索引齐备；固定 GUID 种子；初始管理员（环境变量+首启重置）；迁移可 review | AdminSeedServiceTests 5；集成 fixture 启动即迁移 |
| T2 | 4ce66c3 + fix 1461ba2 | 认证权限全套 + RequirePermission | ✅ login/refresh/logout/me/users/roles/permissions；过期换新；403；me 不重签发 | AuthServiceTests 16、TokenServiceTests 4、Argon2 2、AuthApiTests 8 |
| T3 | 73c66c8 | 编号服务 PG 原子自增 | ✅ 并发 50 无重复、耗尽、格式；Npgsql 命名参数 | NumberingServiceTests 9、NumberingIntegrationTests 5（真实 PG） |
| T4 | c9f6602 | filter DSL 接入全 Search 服务 | ✅ 13 操作符真实现；白名单外 400；排序兜底 | QueryServiceTests 17、QueryIntegrationTests 1（PG 翻译） |
| T5 | 0bd264e | 主数据 API | ✅ CRUD/409/引用保护/嵌套 batches/search/快捷搜索 | MasterDataServiceTests 17、MasterDataApiTests 3 |
| T6 | aafbcf2 | 导入两阶段 | ✅ precheck 不落库 + inline ≤200 + execute 同事务真实入库 | ImportExportServiceTests 6、ImportExportIntegrationTests 2（E2E） |
| T7 | 26c3cbb | 异步导出 | ✅ filter/sort/pageSize 生效；PROCESSING→DONE；独立作用域 | ImportExportIntegrationTests 2（E2E）+ ImportExportApiTests 1 |
| T8 | 08c3430 + fix 7aa8f4d | 幂等接线写端点 | ✅ 同 key 首次结果（含错误）；并发仅执行一次；TTL 24h | IdempotencyServiceTests 4、IdempotencyApiTests 4 |
| T9 | 8519406 | 集成测试+覆盖率 | ✅ WebApplicationFactory+Testcontainers 全链路；覆盖率核心 79.1% | 单元 93 + 集成 29 全绿；coverage 报告 |
| T10 | 本次提交 | 完工汇报 | ✅ 本文件 + 问题清单 | — |

## 复验清单逐条闭环（对照《2026-08-10-后端重做复验意见-第3批.md》）

| 复验意见条目 | 修复证据 |
|---|---|
| B-01 迁移“可应用”无验证、集成测试项目为空 | ✅ AWms.IntegrationTests 现 29 用例；PostgreSqlFixture/ApiTestFixture 启动即 `MigrateAsync()` 实测；`dotnet test AWms.slnx` 全绿 |
| B-02 新库无初始账号（P0） | ✅ AdminSeedService（环境变量 AWMS_ADMIN_USERNAME/PASSWORD 注入 + ResetOnStartup 首启重置）在 Program 启动时执行；AdminSeedServiceTests 5 个；AuthApiTests.Login_初始管理员 实测可登录 |
| B-03 编号 SQL 在 PG 无法执行（P0） | ✅ 重写为 `INSERT…ON CONFLICT…DO UPDATE…RETURNING` + Npgsql 命名参数（@id/@type/@scopeKey/@bizDate）；NumberingIntegrationTests 在 Testcontainers PG 实跑：并发 50 无重复、NUMBER_EXHAUSTED、格式；0 Skip |
| B-04 filter 未接线 + 操作符缺陷（P0） | ✅ QueryService 重写：contains/startsWith JsonElement 转换、isNull 值类型不 500（恒 false/true）、between DateOnly 上界 `AddDays(1)`、嵌套 and/or 合并、主数据默认 id 兜底；MasterDataService 5 个 Search + 嵌套 batches 全部消费 `request.Filter`；白名单外 400（API 实测） |
| B-06 导出丢 filter/sort、无 PROCESSING、捕获请求 DbContext、无 failures 字段、导入 E2E Skip | ✅ ExportRequest 透传 Filter/Sort/PageSize → FilterRequest；状态 PROCESSING（枚举补）→ DONE；后台 `IServiceScopeFactory.CreateAsyncScope()` 独立 DbContext；ImportTaskResponse 增加 `failures`（inline ≤200）；导入 E2E 与导出 E2E 均不 Skip |
| B-07 幂等未接线、无测试 | ✅ 全局 IdempotencyFilter 读取 Idempotency-Key：预留（Key 唯一索引兜底）→ 返回首次结果（含 DomainException 错误响应）→ 24h TTL；IdempotencyApiTests 4 个（重复写/并发/错误/TTL） |
| 操作级权限未实施 | ✅ RequirePermissionAttribute 按 permission claim 校验，挂到 users/roles 写端点、主数据写端点、导入/导出；AuthApiTests.RequirePermission_无权限用户写物料_403 实测 |
| 汇报与实现不符 | ✅ 本汇报按模板如实填写；B-04/B-06/B-07/B-08 均有测试与命令输出对应 |
| JWT SecretKey/连接串进 git | ✅ appsettings.json 全占位（SecretKey/Password/ConnectionStrings 为空）；运行/测试经环境变量或 UseSetting 注入 |
| 种子 Guid.NewGuid() | ✅ 种子全部固定 GUID 常量（0000…/1000…/2000…/3000…） |
| refresh 测试不真实 | ✅ RefreshAsync_ExpiredToken_ReturnsNewToken 用负有效期生成真实过期 token（断言 expiresAt < UtcNow），API 层 Refresh_过期token_换新成功 同 |
| /me 每次重新签发 | ✅ GetMeAsync(userId, currentToken) 返回原 token + 原过期时间；单测 + API 实测 token 不变 |
| 排序 dir 未校验/Enum.Parse 500 | ✅ QueryService 校验 dir asc/desc、Enum.TryParse 无效 → 400；ListUsersAsync 同 |
| Get 全量扫描 | ✅ GetUser/GetRole/GetWarehouse/GetSource/GetMaterial/GetBatch 全部直查；控制器不再 int.MaxValue 扫描 |
| POST /api/materials/{id}/batches/search 未实现 | ✅ BatchesController 提供该路由，MATERIAL_NOT_FOUND 404 实测 |
| logout/refresh 黑名单进程内且不生效 | ✅ TokenService 移除静态黑名单；logout 204 无状态（契约允许） |

## 问题清单（完整版见 backend/交付/问题清单.md）

**待裁决项（单独列出）**：
- **Q-01** 批次 filter 白名单含 materialName，Batch 实体无该列 → 建议契约删除或后续加 join/冗余列（P2）。
- **Q-02** 枚举与错误码.md 导入任务状态未列 PROCESSING（实体已补）→ 建议契约一致化补 PROCESSING（P2）。

其余 Q-03~Q-07 均已按建议实现（含环境性说明），详见问题清单。

## 未验证/遗留项（如实，不标 ✅）

- 库位“有货”引用保护（库存表第 5 批后置）。
- 导出任务持久化（HostedService）后置（Q-04）。
- 幂等响应与业务严格同事务后置（Q-03）。
- 本机 Testcontainers 关闭 Ryuk（Docker Hub 不可达；等价回收方案已实现并在本机实测全绿）。
