> 状态：已归档（2026-08-11）——仅供追溯，不作为当前实现依据

# 协调者审核意见 — 第3批验收测试报告（2026-08-11）

> 审核人：协调者（Codex）｜日期：2026-08-11
> 对象：《第3批-验收测试报告.md》（前端 hermes，commit 32a2473）
> 抽查方式：代码核验 + 真实 API/DB 抽查（tst-operator、admin、awms-test-db-1）

## 一、总体结论

**报告质量高：证据充分、只测只报、数据清单规范，前置 A/B 全过、无全局阻塞。**
前端报告基本可信，但抽查发现 **1 处证据不准确（待裁决⑤）** 与 **1 处流程遗留（报告未推送 GitHub，已由协调者补推）**。验收发现的缺陷按严重度分档，建议进入修复迭代。

## 二、抽查结果（逐项）

| 项 | 前端结论 | 协调者抽查 | 结论 |
|---|---|---|---|
| C4① 菜单未按角色过滤 | 后端高 | ✅ 实测复现：tst-operator permissions 仅 inbound 三码，menus.web 仍返回 4 项（dashboard/inbound/master-data/system） | **属实，后端缺陷** |
| F-01 会话恢复旧 token 回写 | 前端中 | ✅ 代码核验：auth-context.tsx 恢复用 `cached.token` 构建会话并回写，覆盖 refresh 后的新 token → 再次 401/refresh（实测 3 次） | **属实，前端缺陷** |
| E5 快捷搜索默认过滤停用（待裁决⑤） | 疑为缺陷 | ❌ **前提不成立**：DB 中 M-D1-04 的 SearchCode 为 **NULL**（非 CSZS）；keyword=CSZS 只返回 M-D1-03 是**正确行为** | **关闭待裁决⑤**；同时 D5“重复 searchCode 可创建可检索”实际未覆盖（无两个 CSZS 行），属测试数据未造对，按契约 v0.5“searchCode 不唯一”接受，可后续补测 |
| 测试数据与 DB | 报告清单 | ✅ 一致：用户 5（含 tst-*）、物料 32（31 ENABLED）、来源 1、批次 2、导入/导出任务留痕（0003~0007） | 属实 |
| 报告推送 | — | ⚠️ 前端 origin 指向本地共享仓库，报告未到 GitHub；已由协调者推送到 origin/review/第3批-验收（32a2473） | 已补推 |

## 三、缺陷分档与修复建议

### 必须修（P1/P2，进入修复迭代）
1. **C4①（后端·高）菜单权限过滤未生效**：MenuDefinition 种子（AWmsDbContext.cs:350-354）未设 RequiredPermissionCode，BuildMenuTree 过滤恒不生效。
   - 修复：种子补齐 `menu.inbound→route.inbound`、`menu.master-data→route.master-data`、`menu.system→route.system`（dashboard 无权限码=所有登录用户可见占位页）；生成新迁移；补测试（OPERATOR me 仅 inbound 菜单、SUPERVISOR inbound+master-data、admin 全量）。
2. **F-01（前端·中）会话恢复回写旧 token**：auth-context 恢复用 `me.token/me.expiresAt`（后端 /me 返回请求头当前 token），不再用 `cached.token` 覆盖；补单测。

### 建议修（P3，随修复迭代一起）
3. **F-02** 占位页包 AppLayout（带侧边栏）或加返回入口，避免死胡同。
4. **F-03** 批控列布尔 i18n（是/否），中英不显示 true/false。
5. **F-04** 导入任务状态补 zh/en key（enums.importTaskStatus.*）或改渲染方式。
6. **待裁决⑥** 新建对话框打开时重置表单（非草稿语义），与 F-03/F-04 同为前端小修。

### 登记后置（不阻塞本批验收，写入后置项登记）
7. **F-05/待裁决④**：服务端错误文案 i18n——契约 2.5 规定错误文案归后端（messages.zh/en.json），本期后端未实现；登记后置，由后端在后续批次按契约补齐（含 Accept-Language）。

## 四、待裁决项处理建议

| # | 待裁决项 | 建议 |
|---|---|---|
| ① | 未实现模块菜单表现 | **采纳占位页方案**（已上线，B3 ✅）；F-02 随修复迭代补导航 |
| ② | B1 已登录访问 / 行为 | 以“已登录 → 工作台占位页”为准；计划 B1 表述为文档笔误，随修复迭代一并修 |
| ③ | 系统管理 UI（用户/角色/权限页） | 本批 API 全通、UI 缺失；**列入后续批次**（建议第4批之后系统管理批），登记后置 |
| ④ | F-05 错误文案 i18n 归属 | 后端负责（契约 2.5），登记后置 |
| ⑤ | E5 快捷搜索过滤停用 | **关闭**（前提不成立，见抽查表） |
| ⑥ | 新建对话框表单值复用 | 新建时清空（修复迭代一并处理） |

## 五、下一步

1. 用户确认修复范围与上述裁决；
2. 开启修复迭代：后端 C4①（种子+迁移+测试）、前端 F-01/F-02/F-03/F-04/⑥（+B1/计划笔误）；
3. PR → CI → CD 更新测试环境；
4. 前端复测 C4①、F-01 及受影响回归（A7/B3/C4/D5），复测通过后本批验收闭环 → 归档。
