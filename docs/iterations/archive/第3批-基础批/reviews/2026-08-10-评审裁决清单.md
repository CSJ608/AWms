> 状态：已归档（2026-08-11）——仅供追溯，不作为当前实现依据

# 评审裁决清单（2026-08-10，C-05 门禁）

> 来源：前端 AI（hermes）《前端开工评审意见》+ 后端 AI《后端开工评审意见》。
> 裁决人：用户（2026-08-10 逐项确认）。状态：已裁决，文档修订已落实（见各文档修订记录）。
> 环境验证：pnpm 11.16.0 + npm registry 可达，**msw 2.15.0 安装成功**（2026-08-10 实测）→ 前端 mock 层采纳 MSW。

## 一、需用户裁决的 5 项

| # | 事项 | 裁决 |
|---|---|---|
| 1 | PDA 单张打印（前端 A-7 冲突） | **接口本期做 + 打印方案 spike + 界面降级预览**；真机驱动（网络热敏/蓝牙）登记后置项 P-01 |
| 2 | 界面规格补充方式（前端 B-4/B-5） | **通用列表页+通用 Dialog 表单模板化** + 补质检/上架 PDA 关键屏线框 |
| 3 | menus.web 图标（前端 B-20） | 注册表加 `iconKey` |
| 4 | 批次浏览（前端 B-6） | 补 `GET /api/batches`（keyword 分页），满足 ref 实体 keyword 规则 |
| 5 | 编号规则注册（后端 B-4） | **本期代码常量注册 + IFormatter 策略**；DB 表化登记后置项 P-04 |

## 二、低风险修订（全部采纳，已落文档）

| 来源 | 内容 | 落点 |
|---|---|---|
| 前端 B-17 | 流水 reason 枚举 | 枚举与错误码 v1.6 |
| 前端 B-18 | batches 补 keyword/全量列表 | 批次契约 v0.4 |
| 前端 B-19 | 入库单/收货补 filter/sort 白名单 | 入库单 v0.3 / 收货 v0.3 |
| 前端 B-21 | filter DSL 固化（in/notIn 数组、between 日期边界、sort 单列） | 通用规范 v1.8 |
| 前端 B-22 | precheck 固定 inline failures ≤200 + failReportUrl | 导入导出 v0.2 |
| 前端 B-23 | AttachmentItem 补 thumbnailUrl | 附件 v0.2 |
| 前端 B-24 | 入库单创建不收 uniqueCodes（打印端点登记） | 入库单 v0.3 / 打印 v0.2 |
| 前端 B-28 | external-labels 参数统一数组 | 打印 v0.2 |
| 前端 B-3 | 物料筛选区升级 SearchField[] | 基础数据规格 v1.1 |
| 前端 B-8/B-13 | 规格/标签示例同步新编号格式 | 首页/收货PDA/标签规范 |
| 前端 B-9 | PDA 手选用 ReferencePicker 轻量 | 收货PDA v1.1 |
| 前端 B-10 | 连扫累加按 defaultQtyPerLabel | 收货PDA v1.1 |
| 前端 B-11 | 扫外标签批次属性回显确认 | 收货PDA v1.1 |
| 前端 B-14 | 打印失败降级路径 | 收货PDA v1.1 + 打印 v0.2 |
| 前端 B-25 | refresh 形态=Bearer 过期 token | 认证权限 v0.2 |
| 前端 B-31 | 401 以响应触发刷新（单飞+排队重放） | 前端实现约定（评审回执） |
| 后端 A-2 | 附件存储分桶策略 | ADR-004 v0.3 / 附件 v0.2 |
| 后端 A-3 | PDF=QuestPDF | ADR-004 v0.3 |
| 后端 A-4 | 密码哈希收敛 Argon2id | ADR-005 v0.3 |
| 后端 B-1 | lineNo 后端分配规则 | 入库单 v0.3 |
| 后端 B-2 | Sequence 跨天 INSERT | 实现约定（评审回执） |
| 后端 B-3 | 生产退料复用批次走"待检" | 收货 v0.3（已明确） |
| 后端 B-6 | Batch 状态枚举 | 枚举与错误码 v1.6 |
| 后端 B-7 | Receipt 状态流转显式化 | 收货 v0.3 |
| 后端 C-2 | 复式记账应用层校验 SUM=0 | 实现约定（评审回执） |
| 后端 C-4 | Idempotency-Key 存储（IdempotencyRecord 表+TTL） | 通用规范 v1.8 |
| 后端 C-7 | 入库单 RECEIVED 同事务推进（方案 A） | 入库单 v0.3 / 收货 v0.3 |
| 后端 C-8 | 导入执行阶段重校验唯一性 | 导入导出 v0.2 |
| 后端 C-9 | 删除引用检查在事务内 | 实现约定（评审回执） |
| 后端 C-11 | QueryParser 两层实现 | 实现约定（评审回执） |

## 三、前端技术选型（已确认）

React 19 + Vite + TS strict + Tailwind v4 + shadcn/ui + TanStack Query + RHF/zod + TanStack Table + i18next + Vitest + RTL；**MSW mock 层（已验证可装）**；双路由树（web/pda）；ScanInput 抽象（专用 PDA 扫描头=键盘为主 + 摄像头 BarcodeDetector/zxing 兜底）；PWA 预留（vite-plugin-pwa，不缓存 API）；401 单飞刷新+并发排队重放；decimal.js + dayjs；Idempotency-Key 按用户动作粒度注入。

## 四、后置项（登记防遗漏，见 docs/product/后置项登记.md）

P-01 PDA 真机打印 spike；P-02 暗色主题；P-03 PWA 离线逻辑；P-04 编号规则 DB 表化；P-05 数据权限；P-06 字段级权限；P-07 手动每张不均匀拆分；P-08 照片批量导出；P-09 ASN；P-10 免检/抽检规则；P-11 托盘码；P-12 唯一码全局登记；P-13 仓内调拨/移库；P-14 AGV；P-15 HU 个体层；P-16 出库链/盘点/冻结/预留；P-17 报表与自定义列方案；P-18 打印模板配置界面；P-19 运行时编号规则调整；P-20 摄像头扫码可选依赖。

## 五、修订记录

| 日期 | 变更 |
|---|---|
| 2026-08-10 | 初始：C-05 裁决完成 |
