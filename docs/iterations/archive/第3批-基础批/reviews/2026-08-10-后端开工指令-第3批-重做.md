> 状态：已归档（2026-08-11）——仅供追溯，不作为当前实现依据

# 后端开工指令 — 第3批重做（转发给后端 AI）

> 由协调者（Codex）2026-08-10 出具，请在后端工作区 C:\build\b 执行。任务完成后 push 分支并回报。

## 一、命令（PowerShell，按序执行）

```powershell
cd C:\build\b

# 1. 拉取最新 main（契约 v2.1、后端工程规范 v1.3、ADR-006 都在里面）
git fetch origin
git checkout main
git pull origin main

# 2. 从最新 main 开重做分支（不要动旧分支 feat/backend-3，保留作废代码供参考）
git checkout -b feat/backend-3-rework origin/main
git status
```

## 二、开工必读（按顺序）

1. `AGENTS.md`
2. `docs/methodology/_handoff.md`（交接快照）
3. `docs/iterations/active/迭代计划-第3批-基础批.md`
4. `docs/api/` 全套——README（通用规范 **v2.1**：POST /search、排序唯一性兜底）、物料 v0.4、仓库库位 v0.3、来源 v0.3、批次 v0.6、导入导出 v0.2、认证权限 v0.2、枚举与错误码
5. **`docs/guides/后端工程规范.md`（v1.3）**——本轮做法总依据
6. `docs/decisions/ADR-006-主键采用UUIDv7.md`（已转正）
7. `docs/reviews/2026-08-10-后端完工评审意见-第3批.md`（R-01~R-10 终裁 + 复验清单）

## 三、本轮任务（重做 B-01~B-08）

- 按规范 v1.3 从零实现：四层结构 + CPM/global.json/Directory.Build.props、**EF Core Migrations（禁 EnsureCreated）**、POST /api/{resource}/search、UUID v7（禁手动 Guid.NewGuid）、分页排序唯一性兜底、索引齐备、LoginResponse 契约形状、refresh 过期换新、users/roles/permissions/logout/me、编号原子自增（UPDATE...RETURNING）、filter 白名单外 400 + 全操作符、重复码 409、导入 execute 真实入库 + 异步导出 PROCESSING→DONE。
- 测试：单元 + 集成（WebApplicationFactory + Testcontainers PostgreSQL 真实迁移），对照规范 §5.3 必测清单（含并发取号、耗尽、翻页稳定性、导入 E2E）；`dotnet build` 0 error 0 warning、`dotnet test` 全绿。
- 技能：确认 /plugin 已装官方 dotnet 系列（dotnet/aspnetcore/data/msbuild/test/nuget），按规范 §1 使用；契约与技能冲突时以契约为准并回报。
- **docs/api/ 只读**，不许改契约；发现问题写问题清单。
- 完成后：完工汇报写到 `docs/reviews/2026-08-10-后端完工汇报-第3批-重做.md`（按 §8.8 六项模板：范围对照/文件清单/测试结果/契约偏差/遗留问题/耗时，逐条对应需求 + 测试 + 迁移说明 + 技能使用说明），提交并 push 分支。

## 四、自测门禁

```powershell
dotnet build   # 0 error 0 warning
dotnet test    # 单元 + 集成全绿
```

## 五、提交

- Conventional Commits + 中文描述，分项提交（feat:/test:/docs:/fix:）。
- `git push -u origin feat/backend-3-rework`
- 不 push main；不互相改对方代码；旧分支保留别删。
