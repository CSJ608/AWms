# 前后端协作与联调机制（草案 v0.1）

> 状态：**草案（2026-08-10，讨论中）**。确认后并入《规章制度》。
> 适用：后端 AI、前端 AI、协调者（AI）、人（评审/裁决）——多 AI 工具协作。

## 一、角色与职责

| 角色 | 职责 |
|---|---|
| 你（人） | 方向、门禁裁决、最终验收；不写代码细节 |
| 协调者（AI，如 Codex） | 维护文档流转、收集评审意见、裁决问题归属、管交接快照 |
| 后端 AI | 契约实现、领域逻辑、测试、测试环境可用 |
| 前端 AI | mock 数据层、页面实现、**联调执行（默认集成负责人）** |

## 二、开工门禁（每批开工前）

1. 本批设计/契约产物就绪（概念 / 契约 / 界面规格 / 标签规范）。
2. **双向评审**：前端 AI 与后端 AI 各自通读全部相关文档，输出《开工评审意见》（冲突点 / 缺口 / 建议 / 无异议）。
3. 协调者汇总 → 你逐条决议（采纳 / 拒绝 / 延后）→ **先更新标准文档**。
4. 门禁：**双方无异议 + 契约锁定** → 前后端一起开工。
5. 评审记录入 docs/reviews/。

## 三、双轨并行

- 前端基于 **mock 数据层**（与契约严格一致）开发，不依赖后端环境。
- 后端按契约实现 + 测试；两端独立提交、独立测试绿。
- 契约是唯一依据；发现缺口走"需求回流"（先改契约再改实现），**不得自造字段/端点**。

## 四、联调（默认前端 AI 负责）

- 每批指定**集成负责人**（默认前端 AI），在批次计划中写明。
- 联调内容：
  1. 环境就绪（后端 AI 负责启动测试环境 + 迁移 + 冒烟，集成负责人验证）；
  2. 种子数据（集成负责人经真实 API 造，幂等可重跑）；
  3. 全链路跑通（本批工作流的 Given-When-Then 用例）；
  4. 数据核对（UI 与库一致）、控制台零报错。
- 产出：联调记录 + 问题清单（入批次计划"反馈与决议"）。

## 五、问题归口

| 问题类型 | 谁改 | 流程 |
|---|---|---|
| 契约问题（字段/语义/错误码） | **先改契约文档**（提出方起草、协调者确认），实现方按新契约改 | 需求回流单 |
| 后端 bug（实现与契约不符） | 后端 AI + 回归测试 | 问题清单 |
| 前端 bug（与界面规格不符） | 前端 AI | 问题清单 |
| 环境/部署问题 | 后端 AI 修复，集成负责人验证 | 问题清单 |
| 优先级 | P0（数据错误/流程阻塞）当日处理；P1 本批内；P2/P3 记后续批 | — |

## 六、验收与归档

1. 集成负责人提交联调报告；
2. **你验收**：功能（Given-When-Then）+ 视觉（对照界面规格截图/录屏）；
3. 填验收记录 → 批次归档；未通过 → 问题清单回流，修复后复验。

## 七、沟通载体（AI 之间不直接改对方代码）

| 载体 | 用途 |
|---|---|
| docs/api/ 契约 | 唯一依据 |
| docs/iterations/active/ 批次计划 | 当前目标 + 问题清单/反馈决议 |
| docs/methodology/_handoff.md | 状态快照（不入 git） |
| docs/reviews/ | 评审/联调记录 |

## 八、待确认

1. 联调负责人默认前端 AI——确认？
2. 开工门禁"双向评审 + 协调者汇总 + 你裁决"——确认？
3. 问题归口规则（契约先改文档 / 后端 bug 后端改 / 前端 bug 前端改）——确认？
4. 验收以你为准（集成负责人只出报告）——确认？

## 九、修订记录

| 日期 | 变更 |
|---|---|
| 2026-08-10 | v0.1 初始草案 |
