# 后端收尾指令 — NU1903 依赖漏洞修复（第3批合并前最后一项）

> 出具人：协调者（Codex）｜日期：2026-08-11
> 适用：后端工作区 C:\build\b（分支 `feat/backend-3-rework-2`）
> 依据：docs/reviews/2026-08-11-后端联调修复复验通过-第3批.md（复验通过，仅剩 NU1903 必须收尾）

## 一、背景

- 复验通过（Q1/Q2/Q3/Q4/Q6 闭环，135 用例全绿）。
- 唯一剩余项：NuGet 审计 NU1903 —— `Microsoft.AspNetCore.OpenApi 10.0.4` 传递依赖 `Microsoft.OpenApi 2.0.0`，命中安全公告 GHSA-v5pm-xwqc-g5wc（受影响区间 `>=2.0.0-preview.11, <=2.7.4`，**首个修复版本 2.7.5**）。
- 因项目 `TreatWarningsAsErrors=true`，全新 restore 的默认 `dotnet build` 会因 NU1903 失败；此前靠 `-p:NuGetAudit=false` 绕过。**本次修复后默认构建（含审计）必须 0 error 0 warning。**

## 二、命令（PowerShell，按序执行）

```powershell
cd C:\build\b
git fetch origin
git checkout feat/backend-3-rework-2
git pull origin feat/backend-3-rework-2
git status   # 确认工作区干净
```

## 三、修复内容（一个 commit）

1. `backend/Directory.Packages.props`（Utilities 组）增加：

```xml
<PackageVersion Include="Microsoft.OpenApi" Version="2.11.0" />
```

2. `backend/AWms.Api/AWms.Api.csproj` 增加直接引用（传递依赖统一提升到修复版）：

```xml
<PackageReference Include="Microsoft.OpenApi" />
```

3. 验证（**默认命令，不带任何审计跳过标志**）：

```powershell
dotnet build AWms.slnx
# 期望：0 error 0 warning（NuGet 审计通过，无 NU1903）

dotnet test AWms.slnx --no-build
# 期望：单元 93 + 集成 42 = 135 全绿，0 Skip
```

> 若仍有其他 NU1903/NU1xxx 审计告警，不要用跳过标志掩盖，逐个修复或如实记录到汇报。

## 四、汇报

在 `backend/交付/第3批-联调问题修复汇报.md` 的“问题清单逐条闭环”中追加一行：

| NU1903（合并前收尾） | 已修复 | <commit> | Microsoft.OpenApi 2.0.0 → 2.11.0（GHSA-v5pm-xwqc-g5wc 修复版 ≥2.7.5）；默认 `dotnet build`（含审计）0 error 0 warning；`dotnet test` 135 全绿 0 Skip | 默认构建+全量测试 |

```powershell
git add -A
git commit -m "fix(deps): Microsoft.OpenApi升级2.11.0修复NU1903（GHSA-v5pm-xwqc-g5wc，默认构建含审计0 error 0 warning）"
git push -u origin feat/backend-3-rework-2
```

## 五、纪律

- 只改依赖配置；不碰业务代码/契约（docs/ 只读）。
- 不改 .gitignore、不加跳过审计的参数进项目文件。
- 汇报后等待协调者最终合并。