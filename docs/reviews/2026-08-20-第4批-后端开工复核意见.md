# 第 4 批“收货链”后端开工复核意见

> 日期：2026-08-20
> 复核人：后端 AI
> 复核分支：`origin/review/第4批-收货链`
> 复核提交：`84c5c8a0e26e028a18d209852a7a45f77618fccc`
> 原评审基线：`0c8efc4 docs: 建立第4批收货链开工评审基线`
> 复核方式：按要求执行 `git fetch origin`、`git rev-parse origin/review/第4批-收货链`，并通过 `git diff 0c8efc4..84c5c8a` 与 `git show 84c5c8a:<文件路径>` 审阅远端提交；未切换分支，未实现代码，未修改标准文档，未提交或推送。

## 一、结论

**无异议，可以锁定。**

本轮 `84c5c8a` 已将原后端开工评审 P0-1～P0-5 的阻塞点回写到标准契约与数据模型中。当前文档仍标记“待前后端复核”属于门禁状态，不构成本次异议；从后端契约角度看，第 4 批“收货链”已经具备锁定后进入 B-01～B-08 实现的条件。

实现阶段仍需严格落实文档中已写明的并发、幂等、事务闭合与 PostgreSQL 集成测试要求；这些是实现验收项，不再是契约缺口。

## 二、原 P0 问题闭合复核

| 原问题 | 复核结论 | 证据与判断 |
|---|---|---|
| P0-1 有预建单收货缺少批次属性承载 | 已闭合 | `docs/api/收货.md:10` 明确有单/无单行均支持 `batchId? / batchProps?` 且互斥；`docs/api/收货.md:138-143` 补齐有预建单请求字段、`batchProps` 来源、`batchId` 仅 PR 生产退料复用、来源继承与 `SOURCE_MISMATCH`；`docs/api/标签解析.md:64,82` 明确 t=S/外部条码返回 `batchProps` 供收货请求带回。后端不再需要自造字段接收供应商批号、生产日期、效期和来源。 |
| P0-2 唯一码数量模型不足，库存守恒不可验证 | 已闭合 | `docs/api/数据模型-草案.md:35` 为 `UniqueCode` 增加 `quantity`；`docs/api/入库单.md:39,44` 详情回显每码 quantity；`docs/api/打印.md:60-66,102` 明确 `unique-labels` 按 `qtyPerCode` 登记数量且编号、UniqueCode、PrintJob/Item 同事务；`docs/api/收货.md:11,144` 要求行 `quantity` 等于唯一码数量之和，不一致返回 `UNIQUE_CODE_QTY_MISMATCH`。数量守恒校验有字段、有规则、有错误码。 |
| P0-3 单据二维码解析结果不足以建立 PDA 收货上下文 | 已闭合 | `docs/api/标签解析.md:40-47` 的 `document` DTO 已含 `inboundOrderId`、单据类型/号、仓库、状态和行列表；`docs/api/标签解析.md:72,85` 明确 D 单据码一次返回完整上下文，前端不得只靠 `docNo` 二次猜测；`docs/api/标签解析.md:96-102` 将不在单、已收/已作废、仓库不符、来源不符等风险设为 `blocking=true`，且业务接口仍需重复校验。扫码上下文与阻断 warning 已闭合。 |
| P0-4 打印作业模型与文件端点不闭合 | 已闭合 | `docs/api/数据模型-草案.md:53-54,68` 用 `PrintJob/PrintJobItem` 替代语义重叠的 `PrintLog` 并说明 PDF `filePath` 生命周期；`docs/api/打印.md:15-17,24-45` 补打印作业查询、详情、失败重试与完整 DTO；`docs/api/打印.md:83-95` 补文件端点、历史恢复和 retry 语义；`docs/api/打印.md:100-109` 补持久化、失败隔离、文件生命周期和幂等；`docs/api/枚举与错误码.md:93` 补打印错误码。打印历史、失败重试、补打和文件状态有稳定模型。 |
| P0-5 缺少关键唯一约束和并发防重设计 | 已闭合 | `docs/api/数据模型-草案.md:73-86` 补 `InboundOrderLine(orderId,lineNo)`、`UniqueCode(code)`、`ReceiptLine(receiptId,lineNo)`、`QualityCheck(receiptLineId)`、`PutawayRecord(receiptLineId)`、`StockLedger(txnGroupId,seq)`、`PrintJobItem(printJobId,seq)`、`IdempotencyRecord(key)` 等关键唯一约束；`docs/api/数据模型-草案.md:98-101` 明确不同幂等键并发双提交最多一个成功、失败不得留下部分写入、同幂等键重放返回首次结果。后端可据此设计迁移、状态条件更新和集成测试。 |

## 三、专项复核

1. **批次属性、唯一码 quantity 与数量守恒：通过。** 有单/无单批次属性字段、`batchId` 复用边界、来源一致性、唯一码登记数量、收货数量求和校验和错误码均已写入契约。
2. **单据码完整上下文及 blocking warning：通过。** `DOCUMENT_QR` 已返回 `inboundOrderId`、仓库、状态和完整行列表；阻断类 warning 结构清晰，且要求最终写接口重复校验。
3. **PrintJob/PrintJobItem、历史查询、失败重试和打印幂等：通过。** 作业、项目、文件、历史查询、详情恢复、retry、补打边界、FAILED/READY 行为和 `Idempotency-Key` 要求均已闭合。
4. **附件归属、来源校验、唯一约束和并发事务要求：通过。** 附件明确 `RECEIPT/receiptId` 与 `EXCEPTION/qualityCheckId` 归属，业务事务内校验未关联且属于当前用户；PO/PR/OT 来源主数据规则和 `SOURCE_MISMATCH` 已补齐；关键唯一约束、索引与并发验收已进入数据模型。
5. **B-01～B-08 锁定后开工条件：具备。** 第 4 批计划 `docs/iterations/active/迭代计划-第4批-收货链.md:50-57` 的 B-01～B-08 均可在当前契约中找到实现依据；计划 `:119-136` 已把原问题回写为复核门禁，待双方无异议后即可由协调者统一标记锁定并发布开工指令。

## 四、B-01～B-08 逐项判断

| ID | 复核结论 | 说明 |
|---|---|---|
| B-01 入库链与库存写侧模型 | 可开工 | 实体、PrintJob/Item、IdempotencyRecord、唯一约束、索引和并发验收均已补齐，可进入迁移与领域模型设计。 |
| B-02 入库单 | 可开工 | PO/PR/OT 来源规则、DTO 可空性、唯一码由打印登记、状态推进和作废边界明确。 |
| B-03 标签解析与收货 | 可开工 | 单据码上下文、外标签批次属性、唯一码数量、暂存库位校验、来源一致性、收货事务入账规则闭合。 |
| B-04 附件 | 可开工 | 上传、缩略图、查看、删除保护、业务关联归属、所有权与权限边界明确。 |
| B-05 质检 | 可开工 | PASS/EXCEPTION/主管 PASS/REJECT、照片归属、重复处理约束和库存同事务转换明确。 |
| B-06 上架 | 可开工 | 待办、推荐规则、合格库位校验、整行上架、`expectedInventoryVersion` 与 `VERSION_CONFLICT` 明确。 |
| B-07 打印 | 可开工 | 五类打印、PDF/预览降级、作业持久化、历史查询、失败重试、补打与幂等边界明确。 |
| B-08 权限与测试 | 可开工 | 8 个操作权限、Web/PDA 菜单显示条件、并发/幂等/库存守恒集成测试要求均已明确。 |

## 五、实现注意（非锁定异议）

- 收货、质检、异常处理、上架、唯一码登记与打印生成必须按 `docs/api/数据模型-草案.md:69,98-101` 做事务闭合和并发双提交测试；不要把全局幂等过滤器当作库存入账的唯一防重手段。
- 附件关联实现建议使用“未关联 + 上传人匹配”的条件更新或行锁，确保同一 uploadId 在并发业务提交中最多被一个业务对象认领。
- 唯一码无单全局登记已在本批明确不做；实现无单 PR/OT 时若遇到唯一码物料，应按当前收货契约的 `uniqueCodes` 要求和业务边界拒绝，而不是新增全局登记能力。
