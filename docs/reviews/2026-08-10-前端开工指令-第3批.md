# 前端开工指令 — 第3批适配（转发给前端 AI）

> 由协调者（Codex）2026-08-10 出具，请在前端工作区 C:\build\f 执行。任务完成后 push 分支并回报。

## 一、命令（PowerShell，按序执行）

```powershell
cd C:\build\f

# 1. 拉取最新 main（契约 v2.1、评审终裁都在里面）
git fetch origin

# 2. 回到你自己的代码分支并合入最新 main（文档以 main 为准，有冲突保留 main 版本）
git checkout feat/frontend-3
git merge origin/main

# 3. 开重做分支继续改
git checkout -b feat/frontend-3-rework

# 4. 清理测试残留文件（可安全删除）
Remove-Item -LiteralPath "frontend\.node-storage","frontend\.node-storage-shm","frontend\.node-storage-wal" -ErrorAction SilentlyContinue
git status
```

## 二、开工必读

1. `AGENTS.md` + `docs/methodology/_handoff.md`
2. `docs/api/README.md` 通用规范 **v2.1**（重点：列表查询改 **POST /api/{resource}/search**，GET keyword 仅作快捷搜索；排序唯一性兜底）
3. `docs/api/物料 v0.4 / 仓库库位 v0.3 / 来源 v0.3 / 批次 v0.6`
4. `docs/reviews/2026-08-10-前端完工评审意见-第3批.md`（F-R1/F-R2 终裁）

## 三、本轮任务

1. **F-R1（必须）**：`src/i18n/index.ts` 与 `src/platform/auth/session-store.ts` 的裸 `localStorage` 改为 `window.localStorage` + `typeof` 守卫；`package.json` 加 `engines`（node >=22）+ `packageManager`，补 `.nvmrc`；**在当前 Node 26 环境三连复验必须全绿**（Node 22 也要绿）。
2. **F-R2（必须）**：适配契约 v2.1——
   - 列表/高级筛选统一调用 `POST /api/{resource}/search`（body：`{ keyword?, 固定参数..., filter?, sort?, page?, pageSize? }`）；`queryToParams` 改为构造 search body，不再把 filter/sort 塞 URL query；
   - MSW `handlers.ts`/`db.ts` 同步改为 POST /search；引用选择器快捷搜索保留 `GET /api/{resource}?keyword=&pageSize=`；
   - `types.ts` 补 SearchRequest/查询 body 类型。
3. 同步更新相关单测/交互测试（登录、物料 CRUD、筛选、ReferencePicker、导入导出、菜单权限），**pnpm build / lint / test 全绿**。
4. `docs/api/` 只读，不许改契约；发现契约问题写问题清单。
5. 完成后更新 `frontend/第3批-完工汇报.md`（补：Node 版本验证、POST /search 适配说明、测试结果），提交并 push 分支。

## 四、自测门禁（当前环境 Node 26 下必须绿）

```powershell
pnpm build
pnpm lint
pnpm test
```

## 五、提交

- Conventional Commits + 中文描述。
- `git push -u origin feat/frontend-3-rework`
- 不 push main；不互相改对方代码；旧分支保留别删。