# 前端重做复验确认 — 第3批（feat/frontend-3-rework）

> 复核人：协调者（Codex）｜日期：2026-08-10
> 复核对象：`feat/frontend-3-rework`（commit `c294647`）+ 《第3批-完工汇报.md》（frontend/第3批-完工汇报.md，v2 含返工记录）
> 复核依据：docs/reviews/2026-08-10-前端完工评审意见-第3批.md「四、复验标准」+ 终裁（F-R2 改 POST /search）
> 实际复验环境：Node v26.7.0 / pnpm 11.10.0

## 结论（先行）

**通过。** F-R1、F-R2 均已落实并经实际复验属实；完工汇报已按复验标准更新。可进入联调（联调以真实后端为准，注意后端导出/幂等/初始账号等未达项，见后端复验意见）。

## 一、复验标准逐条结果

- [x] **当前环境 `pnpm test` 全绿（不依赖 NODE_OPTIONS 补丁）**：实测 57 passed / 9 files / 0 failed（Node v26.7.0，未加任何 NODE_OPTIONS）。
- [x] **`pnpm build` 保持绿**：实测 0 error（tsc -b + vite build 通过；仅有 chunk >500KB 提示，非错误）。
- [x] **`pnpm lint` 保持绿**：实测 0 error / 7 warning（react fast-refresh 提示，与上轮一致）。
- [x] **版本锁定**：package.json `engines: { node: ">=22" }` + `packageManager: pnpm@11.10.0`，`.nvmrc`（22）；汇报已注明验证版本 v22.23.2 / v26.7.0。
- [x] **F-R1 localStorage 兼容**：`src/lib/storage.ts` 新增 `getLocalStorage()`（window 守卫 + try/catch）；`i18n/index.ts`、`session-store.ts` 全部改走守卫；`test/setup.ts` 在 window.localStorage 缺失时注入内存 Storage 并在 afterEach 清理。实测 Node 26 下测试全绿，说明修复有效。
- [x] **F-R2 列表查询改 POST /search**：`api/index.ts` 6 个标准列表端点全部 POST .../search（toSearchBody，filter/sort 走 body 不进 URL），新增 4 个 GET 快捷搜索（keyword/pageSize≤10）；`mocks/handlers.ts` parseSearchBody/parseQuickQuery 分离；`types.ts` SearchRequest/ListQuery 对齐；`BatchesPage` 默认排序改 createdAt DESC；`db.test.ts` 新增 3 个传输契约守门用例（POST /search 且 filter/sort 不进 URL、嵌套 locations/search + GET 快捷搜索、批次默认 createdAt DESC）。
- [x] **汇报更新**：⑦ 返工记录含根因、修复内容、双环境验证结论；复验标准 4 项全部勾选并附证据。

## 二、说明项（不阻塞，联调时核对）

1. mock 的 import execute 仍不真正写 mock 库（仅置 DONE）——汇报 ⑤ 已注明；联调以真实后端为准（后端 execute 代码已实现真实入库，但缺 E2E 验证）。
2. 导出任务 mock 立即 DONE（真实后端异步 PROCESSING→DONE）——汇报 ⑤ 已注明；前端轮询逻辑已预留。
3. `ExportRequest` 前端已带 filter/sort 发送，但**后端控制器丢弃了 filter/sort**（见后端复验意见），联调前需后端修复。
4. lint 7 个 warning 与构建 chunk 体积提示为已知非阻塞项，建议后续消解。

## 三、结论

前端 F-R1/F-R2 复验通过，作为本轮可接受；联调入口待后端复验通过后开启。