# 后端重做复验通过意见 — 第3批（feat/backend-3-rework-2）

> 复核人：协调者（Codex）｜日期：2026-08-11
> 复核对象：`feat/backend-3-rework-2`（14 个任务化提交，T0~T10 + 3 个修复）+ 《backend/交付/第3批-重做完工汇报.md》/TASKS.md/问题清单.md/coverage
> 复核依据：《后端开工指令-第3批-重做（任务拆分版 v2）》、docs/guides/后端工程规范.md（v1.3）、docs/api/ 契约、docs/reviews/2026-08-10-后端重做复验意见-第3批.md

## 结论（先行）

**通过（复验通过）。** 上轮复验意见中的全部阻塞项（P0/P1）均已修复并有真实测试证据；任务化提交流程执行到位（14 个提交，每任务一提交、目标/验收可核对）；汇报与实现一致、遗留与待裁决如实标注。**本轮代码可以进入联调**；进入合并前需先完成 Q-01/Q-02 两项契约裁决（见下）。

## 一、实际复验（本机执行，非只读汇报）

| 项目 | 结果 |
|---|---|
| `dotnet build AWms.slnx` | ✅ 0 error / 0 warning |
| `dotnet test AWms.Tests` | ✅ 93 通过 / 0 失败 / 0 跳过 |
| `dotnet test AWms.IntegrationTests`（Testcontainers PostgreSQL 16 真实容器） | ✅ 29 通过 / 0 失败 / 0 跳过 |
| 合计 | 122 用例全绿，0 Skip（与汇报一致） |

集成测试在本机 Docker 守护进程（Docker Desktop）下实测通过，证明：迁移可应用、编号原子自增在真实 PG 上可执行（并发 50 无重复/耗尽/格式）、导入导出 E2E、幂等并发/TTL、filter 白名单 400 与操作符语义、refresh 过期换新、RequirePermission 403 均为真实验证。

## 二、复验清单逐条结果（对照《2026-08-10-后端重做复验意见-第3批.md》）

- [x] B-01 迁移可应用：集成测试 fixture 启动即 MigrateAsync，实测通过；无 EnsureCreated。
- [x] B-02 初始账号：AdminSeedService 环境变量注入 + 首启重置（ResetOnStartup）+ 测试 5 个；API 实测登录成功。
- [x] B-03 编号 SQL：改为 INSERT…ON CONFLICT…DO UPDATE…RETURNING + Npgsql 命名参数；并发 50 / 耗尽 / BATCH 物料作用域 / TXN_GROUP 15 位 / NextN 批量，真实 PG 实测。
- [x] B-04 filter DSL：QueryService 重写（contains/startsWith JsonElement、isNull 值类型恒 false/true、between DateOnly 上界 +1 天、嵌套 and/or 合并）；MasterDataService 5 个 Search + 嵌套 batches 全部消费 request.Filter；白名单外 400 有 API 测试；排序唯一性兜底（主数据 id asc、用户/时间 id DESC）；PG 翻译测试实测。
- [x] B-05 主数据：CRUD/重复码 409/引用保护/keyword/searchCode/嵌套 batches/search/Get 直查；API 实测。
- [x] B-06 导入：precheck 不落业务数据、failures inline ≤200、execute 同事务重校验 + 失败全回滚；导入 E2E 不 Skip。
- [x] B-07 导出：ExportRequest filter/sort/pageSize 透传；PROCESSING→DONE（枚举已补）；后台 IServiceScopeFactory 独立 DbContext；导出 E2E（filter/sort/pageSize 生效、pageSize=0 全量）实测。
- [x] B-08 幂等：全局 IdempotencyFilter 接线写端点；同 key 首次结果（含错误）、并发仅执行一次、TTL 24h 过期可重放；测试 8 个。
- [x] 操作级权限：RequirePermission 挂到 users/roles/主数据写端点/导入导出，403 实测。
- [x] 汇报如实：任务对照表（commit→验收→测试）、复验清单逐条闭环、问题清单结构化、遗留/未验证如实。

## 三、规范（后端工程规范 v1.3）符合性要点

- §0 铁律：契约先行（歧义记入问题清单，未单方改 docs/）；测试为证（122 用例 0 Skip）；技能使用已记录。
- §2.1 四层结构：✅；Domain 仅一个未使用的外部包 UUIDNext（见 P3-3）。
- §2.2 密钥不进 git：appsettings 全占位，JWT/连接串/管理员密码走环境变量或测试注入 ✅。
- §2.3 envelope/错误码/白名单 400 ✅。
- §2.4 幂等/异步：幂等已接线（同事务为近似实现，Q-03 已如实标注）；导出异步 + 独立作用域 ✅。
- §2.5 认证：LoginResponse 契约形状、refresh 过期换新、RequirePermission、初始管理员 ✅。
- §3 迁移/UUID v7/排序兜底/索引：全部符合（33 个索引齐备、无 Guid.NewGuid 生产代码、迁移可 review 可应用）。
- §5 测试：分层（单元/集成）、必测清单全绿、覆盖率核心 79.1% ≥70%（MasterDataService 单文件 68.5%，合并口径达标）。

## 四、问题清单处置建议（需用户裁决）

1. **Q-01（P2，待裁决）批次 filter 白名单 materialName**：Batch 实体无该列，实现将其排除（filter materialName → 400）。**建议裁决：契约 docs/api/批次.md 删除 materialName（本批无批次创建路径，第 4 批建批次时按需加 join/冗余列）**；若保留，需补实体列 + 迁移。
2. **Q-02（P2，待裁决）枚举与错误码.md 未列 PROCESSING**：导入导出.md 已要求 PROCESSING，枚举表遗漏。**建议裁决：枚举与错误码.md 补 PROCESSING（契约一致化）**。
3. 其余 Q-03~Q-07 已按建议实现或属环境/后置说明，无需裁决。

> 契约修订由协调者在用户确认后更新 docs/api/（两处小改），不要求后端返工。

## 五、P3 遗留（不阻塞，记录在案）

1. 异步导出 Task.Run 最小形态，未升级 IHostedService/持久队列（Q-04，已如实）。
2. 幂等“响应与业务同事务”为预留先行近似实现（Q-03，已如实）。
3. Domain 项目引入 UUIDNext 包但代码未使用（规范 §2.1「零外部依赖」轻微违反）→ 建议下个提交删除该包引用。
4. IdempotencyFilter 静态 KeyLocks 字典不清理（24h key 量级小，可接受；后续可换 LRU）。
5. 覆盖率 Cobertura XML（约 36k 行）入库偏重，后续建议仅提交覆盖率报告 md。
6. 库位“有货”删除保护依赖库存表（第 5 批后置，契约允许）。

## 六、结论与下一步

- **后端：通过。**
- 进入**批末联调**（集成负责人=前端 AI）：前端 `VITE_USE_MOCK=false` 切真实后端（环境变量注入 AWMS_ADMIN_*、连接串、JWT 密钥），跑主数据 CRUD + 导入导出 + 登录/刷新冒烟，数据与库核对。
- 合并 main 前：完成 Q-01/Q-02 契约裁决（协调者更新 docs/api/ 两处）；联调通过后按 §8.8 进入人裁决 → 合并 main → 批验收记录 → 归档。