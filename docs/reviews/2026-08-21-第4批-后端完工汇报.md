# 第 4 批“收货链”后端完工汇报

> 日期：2026-08-21
> 分支：`feat/backend-第4批-收货链`
> 基线：`4aeb916 docs: 锁定第4批收货链契约`

## 1. 完成范围

- B-01 入库链与库存写侧模型：新增入库单/行、唯一码、收货/行、质检、上架、库存主体、库位库存、事务组、流水、附件、PrintJob/PrintJobItem；迁移包含锁定契约要求的关键唯一约束与索引。
- B-02 入库单：实现创建、列表查询、详情、作废；落实 PO/PR/OT 来源规则、编号服务、行号后端分配、PO 严格策略和幂等写端点。
- B-03 标签解析与收货：实现 `POST /api/scan/parse` 与 `POST /api/receipts`；支持有单/无单 PR/OT、批次创建/复用、唯一码数量守恒、暂存库位校验、待检库存与收货流水同事务。
- B-04 附件：实现上传、查看、缩略图、列表、删除保护；业务提交内按“未关联 + 上传人匹配”并发认领。
- B-05 质检：实现质检待办、PASS、EXCEPTION、主管 PASS/REJECT；PASS/主管 PASS 同事务执行待检到可用库存转换和流水。
- B-06 上架：实现上架待办、推荐库位、目标库位校验、`expectedInventoryVersion` 乐观并发、暂存到目标库位库存转移与流水。
- B-07 打印：实现单据码、外标签、唯一码、批次标签、PDA 单张批次标签、收货回执、作业查询/详情/retry/file；唯一码登记、PrintJob、PrintJobItem 同事务。
- B-08 权限与测试：新增第 4 批 8 个 action 权限和 PDA 菜单；新增单元测试与 PostgreSQL Testcontainers 集成测试，覆盖状态机、幂等、并发、附件认领、库存守恒和失败无部分写入。

## 2. 实现文件

- Domain：`backend/AWms.Domain/Enums/*` 第 4 批枚举、`backend/AWms.Domain/Entities/*` 入库链/库存/附件/打印实体、`backend/AWms.Domain/Dtos/Inbound|Receipts|Scan|Attachments|Print/*` DTO。
- Infrastructure：`backend/AWms.Infrastructure/Data/AWmsDbContext.cs`、`backend/AWms.Infrastructure/Migrations/20260820170804_AddInboundReceiptChain*.cs`、`backend/AWms.Infrastructure/Migrations/AWmsDbContextModelSnapshot.cs`、`backend/AWms.Infrastructure/Services/InboundOrderService.cs`、`ReceiptService.cs`、`AttachmentService.cs`、`PrintService.cs`、`ScanService.cs`、`IdempotencyService.cs`、`ServiceCollectionExtensions.cs`。
- Api：`backend/AWms.Api/Controllers/InboundOrdersController.cs`、`ReceiptsController.cs`、`ReceiptLinesController.cs`、`QualityTodosController.cs`、`QualityChecksController.cs`、`PutawayTodosController.cs`、`PutawayRecordsController.cs`、`ScanController.cs`、`AttachmentsController.cs`、`PrintController.cs`、`ControllerUserExtensions.cs`。
- Tests：`backend/AWms.Tests/Services/InboundModelTests.cs`、`backend/AWms.IntegrationTests/InboundReceiptChainApiTests.cs`、`backend/AWms.IntegrationTests/ApiTestFixture.cs`。

## 3. 测试结果

- `dotnet build backend\AWms.slnx --no-restore`：通过，0 warning / 0 error。
- `dotnet test backend\AWms.Tests\AWms.Tests.csproj --no-build`：通过，97/97。
- `dotnet test backend\AWms.IntegrationTests\AWms.IntegrationTests.csproj --no-build`：通过，51/51。
- `dotnet test backend\AWms.slnx --no-build`：通过，单元 97/97，PostgreSQL Testcontainers 集成 51/51。

## 4. 契约偏差 / 需求回流

- 无契约偏差。
- 无需求回流。
- 锁定契约与界面规格未修改；未新增字段或端点；未修改前端代码。

## 5. 遗留问题

- PDA 真机打印仍按迭代计划作为 P-01 spike 后置，本批已提供 PDF/二维码内容预览和文件下载基线。
- 服务端错误文案 i18n 仍按 P-22 后置；本批返回稳定中文 message 与契约错误码。

## 6. 耗时

- 约 45 分钟（2026-08-21 00:38 ~ 01:23，Asia/Shanghai）。
