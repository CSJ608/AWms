# 前端收尾指令 — F3 按钮操作权限接回（第3批合并前）

> 出具人：协调者（Codex）｜日期：2026-08-11
> 适用：前端工作区（默认 C:\build\f，分支 `feat/frontend-3-integration`）
> 依据：docs/reviews/2026-08-11-后端联调修复复验通过-第3批.md（后端已补 9 个操作权限码，前端需把按钮显隐接回）

## 一、背景

- 后端已补注册表：`action.warehouse.create/edit/delete`、`action.location.create/edit/delete`、`action.source.create/edit/delete`（moduleCode=master-data；种子：SYSTEM_ADMIN 全量、SUPERVISOR 增加）。
- 前端 mock `src/mocks/seed.ts` 已包含这 9 个码（F1 时保留）；联调期 F3 暂以“不传 permission=默认允许”过渡，**现在接回**。

## 二、命令（PowerShell，按序执行）

```powershell
cd C:\build\f
git fetch origin
git checkout feat/frontend-3-integration
git pull origin feat/frontend-3-integration
git status   # 确认工作区干净
```

## 三、接回内容（一个 commit）

1. **仓库页**（`src/modules/web/master/WarehousesPage.tsx` 及仓库 FormDialog 按钮）：新建/编辑/删除按钮传 `permission="action.warehouse.create" / "action.warehouse.edit" / "action.warehouse.delete"`。
2. **库位页**（`LocationsPage.tsx` 及库位 FormDialog）：`action.location.create / edit / delete`。
3. **来源页**（`SourcesPage.tsx` 及来源 FormDialog）：`action.source.create / edit / delete`。
4. **物料页**保持 `action.material.*`（F1 已对齐，不动）。
5. **权限语义**：按 `MasterListPage`/按钮现有约定——`permission` 有值时按登录用户权限隐藏/禁用；无权限不显示入口。以现有 Material 页的实现为参照，保持风格一致。
6. **mock 角色**：确认 `seed.ts` 中 SUPERVISOR 含新增 9 码（或对应 mock 角色），OPERATOR 保持 inbound 不含这些码；如不一致同步补齐。
7. **测试**：如 `MenuFiltering.test.tsx`/权限相关用例需随按钮权限更新，同步调整并保持断言有意义（不删断言）。

## 四、验证（三连必须全绿）

```powershell
pnpm test    # 期望：57/57（或按新增用例数全绿）0 failed
pnpm build   # 期望：0 error
pnpm lint    # 期望：0 error
```

> 若新增权限用例导致数量变化，如实记录总数。

## 五、汇报与提交

在 `frontend/第3批-联调汇报.md` 的“前端适配修复”表中把 **F3 状态更新为“已接回”**，并注明 commit 与测试结果；同时在“遗留/未验证项”中移除 F3 相关待办。

```powershell
git add -A
git commit -m "feat(F3): 仓库/库位/来源按钮操作权限接回（action.warehouse/location/source.*，与后端注册表对齐）"
git push -u origin feat/frontend-3-integration
```

## 六、纪律

- 只改前端；不碰后端代码与 docs/（docs/ 只读）。
- 不删既有测试断言；行为变化要有测试佐证。
- 完成后等待协调者最终合并（你不需要合并 main）。