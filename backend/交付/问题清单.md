# 问题清单（backend/交付）

> 依据：《后端开工指令-第3批-重做（任务拆分版 v2）》第七节。
> 状态取值：待裁决 / 已按建议实现 / 阻塞。
> 写入纪律：只记契约/规范可定位的证据，不写“感觉”；不单方改 docs/。

| 编号 | 主题 | 出处（契约/规范条款） | 现状与证据 | 建议方案 | 严重度 | 状态 |
|---|---|---|---|---|---|---|
| Q-01 | 批次 filter 白名单含 materialName，但 Batch 实体无该列 | docs/api/批次.md v0.6「字段白名单：materialId、materialCode、materialName、batchNo…」 | Batch 实体仅有 MaterialCode 冗余列（AWms.Domain/Entities/Batch.cs），无 MaterialName；对 materialName 使用 filter DSL 会返回 400 VALIDATION_ERROR（白名单外） | ① 契约删除 materialName；② 或后续批次加 join/冗余列支持（涉及迁移与写路径一致性） | P2 | 待裁决 |
| Q-02 | 枚举与错误码.md 的导入任务状态未列 PROCESSING | docs/api/枚举与错误码.md v1.6「导入任务状态 PRECHECKING/PRECHECKED/EXECUTING/DONE/FAILED」 vs docs/api/导入导出.md v0.2「status=PROCESSING → DONE」 | 契约导出章节要求 PROCESSING，但枚举表未列；实体枚举已补 PROCESSING（T1，ImportTaskStatus.cs），docs 只读未改 | 协调者在枚举与错误码.md 补 PROCESSING（契约一致化） | P2 | 待裁决 |
| Q-03 | 幂等响应记录与业务提交的“同事务”为近似实现 | docs/guides/后端工程规范.md §2.4「记录 TTL（24h），与业务同事务」 | IdempotencyFilter 预留行在业务执行前插入（同请求连接），响应在 action 完成后写入——业务事务已提交后写响应；若进程在两者间崩溃，重试将因 pending 记录返回 409（不会重复执行业务，但拿不到首次结果） | 预留先行已杜绝重复执行；严格“同事务”需在业务事务内写响应，需自定义结果缓存管道（后续批次可做） | P3 | 已按建议实现（预留先行+唯一索引兜底，杜绝重复执行） |
| Q-04 | 异步导出用 Task.Run + 独立 scope，而非 HostedService | docs/guides/后端工程规范.md §2.4「导出任务必须异步执行…不允许请求内同步生成」 | ImportExportService.CreateExportAsync 用 `_ = Task.Run(...)` + IServiceScopeFactory 独立 DbContext（不复用请求作用域），满足“异步执行+独立作用域”；进程重启会丢未完成任务 | 后续批次升级为 IHostedService/后台队列（可持久化任务） | P3 | 已按建议实现（契约最小形态） |
| Q-05 | Testcontainers Ryuk 无法拉取（环境网络） | docs/guides/后端工程规范.md §5.4「集成测试用 Testcontainers」 | 本机 Docker Hub 直连不可达（registry-1.docker.io 连接超时），testcontainers/ryuk:0.9.0 拉取失败 → PostgreSqlFixture/ApiTestFixture 静态关闭 ResourceReaper，容器由 fixture DisposeAsync 显式回收，功能等价 | 联调/CI 环境放开 Docker Hub 后可恢复默认 Ryuk；postgres:16-alpine 镜像本地已具备 | P3（环境） | 已按建议实现（等价方案） |
| Q-06 | 登出无状态实现 | docs/reviews/2026-08-10-后端重做复验意见-第3批.md「logout/refresh 黑名单为进程内静态字典…契约允许无状态」 | AuthController.Logout 返回 204 且不再维护进程内黑名单（TokenService 已移除静态黑名单） | 后续如需“服务端使 token 失效”，引入持久化 token 版本/jti 黑名单 | P3 | 已按建议实现 |
| Q-07 | 批次 SourceType/SourceCode 实体用 string 存储 | docs/api/批次.md v0.6「sourceType ∈ SUPPLIER / WORKSHOP」 | Batch.SourceType 为 string（可为 null），filter 的 eq/in 按字符串比较可用；本批无批次创建端点，强校验随第 4 批收货建批次时补 | 收货批次创建时用枚举校验；如需 DB 层约束再改实体 | P3 | 已按建议实现（本期无创建路径） |
