# 第3批-联调问题修复汇报（Q1/Q2/Q3/Q4/Q6）

> 出具人：后端 AI（Codex）｜日期：2026-08-11
> 分支：`feat/backend-3-rework-2`（在 rework-2 上追加修复提交，未合并、未 push main）
> 依据：docs/reviews/2026-08-11-前端联调抽查意见-第3批.md、docs/reviews/2026-08-11-后端联调修复指令-第3批.md、docs/guides/后端工程规范.md（v1.3）、docs/api/认证权限.md（v0.2）、docs/api/README.md（通用规范 2.1）
> 结论：Q1/Q2/Q3/Q4/Q6 全部闭环；Q5 与 searchCode 唯一性按裁决走契约（后端未改）。`dotnet build` 0 error 0 warning；`dotnet test AWms.slnx` 单元 93 + 集成 42 全绿、0 Skip。

## 一、问题清单逐条闭环

| 项 | 裁决/结论 | Commit | 修复说明 | 测试 |
|---|---|---|---|---|
| Q1（P1）错误 envelope 大小写不一致 | 必须修复 | `bdf8718` | 新增 `AWms.Api/Middleware/ApiJsonOptions.cs`（camelCase + `UnsafeRelaxedJsonEscaping`，与 IdempotencyFilter 一致）；`ExceptionHandlerMiddleware` 两处手写序列化统一走该 options；IdempotencyFilter 复用同一 options（消除重复定义）。全仓排查：仅 ExceptionHandlerMiddleware 存在 `WriteAsync(JsonSerializer.Serialize(...))` 手写点，已统一。 | `ErrorEnvelopeContractTests` 5 例：401 LOGIN_FAILED、409 MATERIAL_CODE_DUPLICATED、422 IMPORT_VALIDATION_FAILED、403 FORBIDDEN、404 NOT_FOUND —— 断言顶层含小写 `code/message/data` 且不含 `Code/Message/Data`，全绿 |
| Q2（P2，已裁决）读端点无模块路由权限 | 读端点挂 `route.<moduleCode>` | `a415ee5` | `[RequirePermission("route.master-data")]` 加到 Materials/Warehouses/Sources/Batches/ImportExport/Meta 控制器类级；`[RequirePermission("route.system")]` 加到 Users/Roles/Permissions 控制器类级；AuthController 除外；RequirePermission 的 AllowAnonymous 豁免保留；类级 route 先于方法级 action 执行（无 route 权限先 403）。 | `PermissionRouteApiTests`：OPERATOR `GET /api/users`、`POST /api/materials/search` → 403；SUPERVISOR 无 route.system → `GET /api/users` 403；admin 读写全部 200 |
| Q3（P2，已裁决）仓库/库位/来源操作权限码缺失/误用 | 补 9 码 + 修正误用 | `a415ee5` | 注册表新增 `action.warehouse.create/edit/delete`、`action.location.create/edit/delete`、`action.source.create/edit/delete`（moduleCode=master-data，固定 GUID 1000…014-022）；种子：SYSTEM_ADMIN 全量（RolePermission 3000…050-058）、SUPERVISOR 增加 9 码（3000…041-049）、OPERATOR 保持 inbound 不变；控制器改码：WarehousesController 写端点用 `action.warehouse.*`、库位写端点用 `action.location.*`，SourcesController 用 `action.source.*`，MaterialsController 保持 `action.material.*`。 | `PermissionRouteApiTests`：SUPERVISOR 写仓库/库位/来源 201 且搜索 200；OPERATOR 写仓库/来源 403；admin 读写全部 200；`GET /api/permissions` 含新 9 码 |
| Q4（P3，应补）`GET /api/meta/fields/users` 未实现 | 补实现 | `02e71c4` | MetaController 增加 `"users"` 分支：`username/name`（string：eq/contains/startsWith/in）、`status`（enum：ACTIVE/DISABLED，eq/in）、`createdAt`（datetime：eq/gt/gte/lt/lte/between）。 | `MetaApiTests` 2 例：存在返回 FieldMeta[]（字段/类型/操作符/枚举选项断言）；未知资源 404 NOT_FOUND |
| Q6（P1）JWT 密钥双来源不一致 | 必须修复 | `080a3c3` | Program.cs 在 `AddInfrastructure`（JwtOptions 绑定）与 JwtBearer 配置前解析密钥并回填：`configuration["Jwt:SecretKey"] = secretKey`（secretKey 仍优先 `Jwt:SecretKey`，否则 `AWMS_JWT_SECRET`；缺失抛错逻辑保留），使 TokenService 与 JwtBearer 共用同一密钥。 | `JwtSecretSourceTests` 2 例：独立 WebApplicationFactory **只注入 `AWMS_JWT_SECRET`**（不设 `Jwt:SecretKey`）→ 登录 200 且 `GET /api/auth/me` 200（原 IDX10703 不再出现）；未配置密钥启动即抛“未配置”异常 |
| Q5（P3，已裁决）`GET /api/roles` 响应形态 | 契约处理，后端未改 | - | 本期返回数组；page/pageSize 预留。契约由协调者更新（认证权限.md 明确响应形态）。 | - |
| searchCode 唯一性（补充记录） | 契约处理，后端未改 | - | 后端已有唯一约束；契约物料/仓库库位/来源补充“searchCode 唯一”说明由协调者更新。 | - |
| NU1903（合并前收尾） | 已修复 | `e6d30d3` | Microsoft.OpenApi 2.0.0 → 2.11.0（GHSA-v5pm-xwqc-g5wc 修复版 ≥2.7.5）；默认 `dotnet build`（含审计）0 error 0 warning；`dotnet test` 135 全绿 0 Skip | 默认构建+全量测试 |

### 额外提交（为全量测试全绿所需，非 Q 项）

| Commit | 说明 |
|---|---|
| `d6d301d` | `NumberingIntegrationTests.NextAsync_耗尽_抛NUMBER_EXHAUSTED` 原断言硬编码 `T-20260810-*`，跨日（2026-08-11）必失败；改为按 `DateTime.UtcNow.ToString("yyyyMMdd")` 断言，保留耗尽/编号行为断言，消除跨日脆弱性。 |

## 二、测试结果（命令与用例数）

```powershell
dotnet build AWms.slnx --no-restore
# 已成功生成。0 个警告，0 个错误

dotnet test AWms.slnx --no-build
# AWms.Tests.dll          已通过：通过 93，失败 0，跳过 0
# AWms.IntegrationTests.dll 已通过：通过 42，失败 0，跳过 0
# 合计：135 通过 / 0 失败 / 0 Skip
```

- 单元测试 93（编号/查询/认证/主数据/导入导出/幂等/AdminSeed/Token/Hasher）；
- 集成测试 42（WebApplicationFactory + Testcontainers PostgreSQL 真实迁移，含本轮新增：ErrorEnvelopeContractTests 5、JwtSecretSourceTests 2、PermissionRouteApiTests 4、MetaApiTests 2；核心用例 0 Skip）。

## 三、迁移说明（F-03）

- 新迁移：`20260811002131_AddMasterDataActionPermissions`（`dotnet ef migrations add` 生成，禁 EnsureCreated）。
- Up 内容摘要（SQL 由 EF 生成的 InsertData 等价）：
  - `Permissions` 插入 9 行：`action.warehouse.create/edit/delete`、`action.location.create/edit/delete`、`action.source.create/edit/delete`（ACTION，moduleCode=master-data，固定 GUID `10000000-…-000014~000022`）；
  - `RolePermissions` 插入 18 行：SUPERVISOR（role `00000000-…-00000002`）9 行（RolePermission GUID `30000000-…-000041~000049`）；SYSTEM_ADMIN（role `00000000-…-00000001`）9 行（`30000000-…-000050~000058`）；
  - Down：按 Id 删除对应 RolePermissions 与 Permissions。
- 迁移文件随代码提交（`AWms.Infrastructure/Migrations/20260811002131_AddMasterDataActionPermissions.cs` + Designer + 模型快照更新），已通过集成测试在真实 PG 上应用验证。

## 四、遗留/未验证项（如实）

1. **前端按钮权限接回（F3）**：本轮只修后端；前端权限码/按钮显隐接回由协调者在合并时另行安排（修复指令声明）。
2. Q5 与 searchCode 唯一性：契约修订由协调者统一处理（docs/ 只读，后端未改）。
3. NuGet 审计 NU1903（Microsoft.OpenApi 2.0.0 已知漏洞）：既有依赖问题，与本次修复无关；本环境 restore 时以 `-p:NuGetAudit=false` 跳过审计（未改任何项目文件、未提交）。
4. 迁移仅在本轮集成测试容器（新库全量迁移）验证；已有库的增量升级（含联调库 awms-pg:5433）未由本端执行，需在合并后由协调者/部署侧跑 `dotnet ef database update`。
5. 运行中的旧后端实例（AWms.Api PID 57156，联调遗留）因锁定构建输出已被停止；如需继续联调请以新构建重新启动。